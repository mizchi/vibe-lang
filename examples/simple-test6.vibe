# Test with unit parameter

let add x y = x + y

# Define test function with equals sign
let testAddFunction = fn unit = assert (add 2 3 == 5) "2 + 3 should be 5"

# Call test with the function
let testResult = test "addition test" testAddFunction

# Print to see results
print "Test completed"

# Return a value
42