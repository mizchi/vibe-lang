# Example file with no tests
# This file should be ignored by test discovery

let add x y = x + y
let subtract x y = x - y
let multiply x y = x * y
let divide x y = x / y

# Just regular functions, no test() calls
let main = add 10 20