# Test without lambda syntax issues

let add x y = x + y

# Define test functions separately
let testAddFunction = fn () -> 
  assert (add 2 3 == 5) "2 + 3 should be 5"

# Call test with the function
let testResult = test "addition test" testAddFunction

# Just return a value
42