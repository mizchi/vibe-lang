(module StringUtils
  (export append get_length to_upper)
  
  (let append (fn (s1 s2) (concat s1 s2)))
  (let get_length (fn (s) 0))    ; Placeholder - returns Int
  (let to_upper (fn (s) s))      ; Placeholder - returns String
)