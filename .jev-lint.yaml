# Build the local grammar first: pkf run jev-parser.
languages:
  vibe:
    libraryPath: .jev-lint/parsers/vibe.dylib
    extensions: [vibe]
    expandoChar: _
rules:
  vibe/fn-name-promises: on
  vibe/test-name-verifies-claim: on
