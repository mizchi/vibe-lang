//# Imports

import ./index.vibe {
  run,
  complete,
  finalize,
}

//# Misc

test "rlm index exports public symbols" {
  let _ = run
  let _ = complete
  let result = finalize("ok")
  assert(result == "ok")
}
