//# Imports

import .{
  Fs::exists, Fs::read_file, Fs::write_file
}

//# Misc

test "fs index exports canonical aliases" {
  let path = "/tmp/vibe_fs_index_alias_test.txt"
  Fs::write_file(path, "ok")
  assert(Fs::exists(path))
  assert(String::equals(Fs::read_file(path), "ok"))
}
