//# Imports

import .{
  fs_exists, fs_read_file, fs_write_file
}

//# Misc

test "fs index exports canonical aliases" {
  let path = "/tmp/vibe_fs_index_alias_test.txt"
  fs_write_file(path, "ok")
  assert(fs_exists(path))
  assert(string_equals(fs_read_file(path), "ok"))
}
