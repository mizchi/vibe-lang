// Filesystem as algebraic effect (replaces with { Net } for fs ops)

export effect Fs {
  ReadFile(String) -> String;
  WriteFile(String, String) -> Unit;
  Exists(String) -> Bool;
  Mkdir(String) -> Unit
}
