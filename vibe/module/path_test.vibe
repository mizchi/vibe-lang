//# Imports

import ./path.vibe {
  dir_of, resolve_path
}

//# Misc

test "dir_of extracts parent directory" {
  assert(String::equals(dir_of("a/b/c.vibe"), "a/b"))
  assert(String::equals(dir_of("main.vibe"), ""))
}

test "resolve_path resolves ./ and appends .vibe" {
  assert(String::equals(resolve_path("a/b", "./mod"), "a/b/mod.vibe"))
  assert(String::equals(resolve_path("", "./main"), "main.vibe"))
}

test "resolve_path resolves ../ relative to parent" {
  assert(String::equals(resolve_path("a/b", "../mod"), "a/mod.vibe"))
  assert(String::equals(resolve_path("a/b", "../mod.vibe"), "a/mod.vibe"))
}

test "resolve_path leaves non-local namespace untouched" {
  assert(String::equals(resolve_path("a/b", "std/prelude"), "std/prelude"))
}
