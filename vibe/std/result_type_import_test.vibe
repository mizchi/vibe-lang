use ./result.vibe {
  Result
}

test "result_type_import_and_method_style" {
  assert(Result::is_ok(Ok(1)))
  assert(Result::is_err(Err("x")))
  assert(eq(Result::unwrap_or(Err("x"), 7), 7))
  assert(Ok(2).is_ok())
  assert(eq(Err("x").or(10), 10))
}
