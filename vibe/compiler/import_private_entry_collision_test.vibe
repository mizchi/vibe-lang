import ./import_private_entry_collision_dep.vibe { answer as dep_answer }

let helper: () -> Int = () -> { 2 }

test "dependency private helper does not collide with entry private helper" {
  assert_eq(dep_answer() + helper(), 3)
}
