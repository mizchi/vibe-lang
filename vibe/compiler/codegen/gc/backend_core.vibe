//# Imports

import .. / .. / core {
  Stmt
}

import ./backend_entry.vibe {
  compile_wasi_module_gc as compile_wasi_module_gc_entry
}

//# Functions

export let compile_wasi_module_gc = (stmts: Array[Stmt], entry_name: String) -> Bytes with { Error } {
  compile_wasi_module_gc_entry(stmts, entry_name)
}
