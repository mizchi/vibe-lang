//# Imports

import .. / .. / core {
  Expr
}

import .. / common_base {
  CompileCtxGc
}

//# Types

export type CompileLambdaGcFn = (Bytes, Expr, Array[String], Int, CompileCtxGc, Int) -> Int with { Error }
