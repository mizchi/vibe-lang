import ../../core/index.vibe {
  Stmt
}

import ./backend_body.vibe {
  compile_wasi_module_gc as compile_wasi_module_gc_impl
}

export let compile_wasi_module_gc = (stmts: Array[Stmt], entry_name: String) -> Bytes with { Error } {
  compile_wasi_module_gc_impl(stmts, entry_name)
}
