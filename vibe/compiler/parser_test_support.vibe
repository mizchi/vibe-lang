//# Imports

import ./lexer.vibe {
  lex
}

import ./parser.vibe {
  parse
}

import ./printer.vibe {
  print_expr
}

//# Functions

export let parse_print = (src: String) -> String with { Error } {
  let tokens = lex(src)
  let expr = parse(tokens)
  print_expr(expr)
}
