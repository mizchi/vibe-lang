//# Imports

import ./core {
  Expr,
  Pat,
  TypeExpr
}

//# Functions

let test_fn: (Expr) -> String = (e) -> {
  match e {
    EInt(n) => "int",
    EFloat(n) => "float",
    EString(s) => s,
    EBool(b) => "bool",
    EIdent(name, _) => name,
    EUnit => "()",
    _ => "other"
  }
}
