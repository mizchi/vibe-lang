//# Imports

import ./ast.vibe {
  Expr
}

//# Functions

let test_fn = (e: Expr) -> String {
  match e {
    EInt(n) => "int",
    EFloat(n) => "float",
    EString(s) => s,
    EBool(b) => "bool",
    EIdent(name) => name,
    EUnit => "()",
    _ => "other"
  }
}
