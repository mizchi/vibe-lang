//# Imports

import ./ast.vibe {
  Expr, Pat, TypeExpr
}

//# Functions

let test_fn: (Expr) -> String = (e) -> {
  match e {
    EInt(n) => "int",
    EFloat(n) => "float",
    EString(s) => s,
    EBool(b) => "bool",
    EIdent(name) => name,
    EUnit => "()",
    _ => "other"
  }
}
