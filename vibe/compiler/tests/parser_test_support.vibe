//# Imports

import ../syntax {
  lex,
  print_expr
}

import ../parser.vibe {
  parse
}


//# Functions

export let parse_print: (String) -> String with { Error } = (src) -> {
  let tokens = lex(src)
  let expr = parse(tokens)
  print_expr(expr)
}
