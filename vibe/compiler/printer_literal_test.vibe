//# Imports

import ./printer_test_support.vibe {
  roundtrip
}

//# Misc

test "roundtrip array" {
  let ok = handle {
    roundtrip("[1, 2, 3]")
  } with Error {
    Throw(_) => false
  }
  assert(ok)
}

test "roundtrip tuple" {
  let ok = handle {
    roundtrip("(1, 2)")
  } with Error {
    Throw(_) => false
  }
  assert(ok)
}

test "roundtrip unit" {
  let ok = handle {
    roundtrip("()")
  } with Error {
    Throw(_) => false
  }
  assert(ok)
}
