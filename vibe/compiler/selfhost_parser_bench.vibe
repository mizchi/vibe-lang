//# Imports

import ./parser_hotspot_probe.vibe {
  probe_parse_checker_vibe, probe_parse_deep_binop_chain, probe_parse_host_parser_vibe, probe_parse_lexer_vibe, probe_parse_parser_vibe, probe_parse_regexp_vibe, probe_parse_wat_encoder_vibe, probe_parse_wide_match
}

//# Misc

bench "selfhost/parse_lexer_vibe" {
  let _ = handle {
    probe_parse_lexer_vibe()
  } with Error {
    Throw(_) => 0
  }
}

bench "selfhost/parse_checker_vibe" {
  let _ = handle {
    probe_parse_checker_vibe()
  } with Error {
    Throw(_) => 0
  }
}

bench "selfhost/parse_parser_vibe" {
  let _ = handle {
    probe_parse_parser_vibe()
  } with Error {
    Throw(_) => 0
  }
}

bench "selfhost/parse_host_parser_vibe" {
  let _ = handle {
    probe_parse_host_parser_vibe()
  } with Error {
    Throw(_) => 0
  }
}

bench "selfhost/parse_regexp_vibe" {
  let _ = handle {
    probe_parse_regexp_vibe()
  } with Error {
    Throw(_) => 0
  }
}

bench "selfhost/parse_wat_encoder_vibe" {
  let _ = handle {
    probe_parse_wat_encoder_vibe()
  } with Error {
    Throw(_) => 0
  }
}

bench "selfhost/parse_deep_binop_chain" {
  let _ = handle {
    probe_parse_deep_binop_chain()
  } with Error {
    Throw(_) => 0
  }
}

bench "selfhost/parse_wide_match" {
  let _ = handle {
    probe_parse_wide_match()
  } with Error {
    Throw(_) => 0
  }
}
