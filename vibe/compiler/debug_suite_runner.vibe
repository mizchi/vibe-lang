test "minimal env test" {
  let path = Env::get("VIBE_SELFHOST_SUITE_REPORT_LIST")
  assert(String::length(path) > 0)
}

test "minimal fs test" {
  let content = Fs::read_file("/etc/hostname")
  assert(String::length(content) > 0)
}
