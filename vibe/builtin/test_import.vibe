//# Imports

use ./bool.vibe {
  bool_to_int
}

use ./float.vibe {
  abs, clamp
}

//# Functions

// a: Int
let a: Int = bool_to_int(true)

// b: Float
let b: Float = abs(__neg(3.5f))

// c: Float
let c: Float = clamp(10f, 0f, 4f)
