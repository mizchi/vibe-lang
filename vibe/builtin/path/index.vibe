export let version = "0.1.0"

export ./ref.vibe {
  type PathRef, type DynamicPath, dynamic, from_literal, PathRef::as_string,
  PathRef::is_absolute, DynamicPath::as_string
}

export ./runtime.vibe {
  resolve, DynamicPath::resolve
}

export {
  PathRef, DynamicPath, dynamic, resolve, from_literal, PathRef::as_string,
  DynamicPath::resolve, PathRef::is_absolute, DynamicPath::as_string
}
