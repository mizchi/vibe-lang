//# Imports

import ./http.vibe {
  request, response_status, response_header, response_body, close,
  listen, accept, request_method, request_url, request_header, request_body, respond
}

//# Misc

test "http module exports are callable" {
  assert(true)
}
