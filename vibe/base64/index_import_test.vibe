//# Imports

import ./index.vibe {
  encode, decode
}

//# Misc

test "base64 index exports core symbols" {
  assert(string_equals(encode("Man"), "TWFu"))
  assert(string_equals(decode("TWFu"), "Man"))
}
