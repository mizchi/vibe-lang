//# Imports

import ./types.vibe {
  type FileEntry
}

//# Misc

test "FileEntry struct" {
  let entry = FileEntry::{
    name: "test", path: "/test", is_dir: false
  }
  assert(entry.name == "test")
}
