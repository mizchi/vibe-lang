//# Imports

export ./math.vibe {
  abs, clamp, comb, divmod, gcd,
  is_even, is_odd, is_power_of_two,
  lcm, log2, max, max_array, min, min_array,
  perm, pow, product, sign, sqrt_int, sum
}

//# Functions

export let version: String = "0.1.0"

//# Misc

export {
  abs, clamp, comb, divmod, gcd,
  is_even, is_odd, is_power_of_two,
  lcm, log2, max, max_array, min, min_array,
  perm, pow, product, sign, sqrt_int, sum
}

export {
  abs, clamp, comb, divmod, gcd,
  is_even, is_odd, is_power_of_two,
  lcm, log2, max, max_array, min, min_array,
  perm, pow, product, sign, sqrt_int, sum, version
}
