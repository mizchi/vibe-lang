//# Imports

import ./parser.vibe {
  ParseResult,
  ArgList,
  ArgToken,
  parse_args,
}

//# Functions

export let demo_parse = () -> ParseResult {
  parse_args([
    "--name=alice",
    "--verbose",
    "-abc",
    "-o",
    "out.txt",
    "--",
    "--literal",
  ])
}
