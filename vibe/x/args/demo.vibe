use ./parser.vibe {
  ParseResult,
  ArgList,
  ArgToken,
  parse_args,
}

export let demo_parse = () -> ParseResult {
  parse_args([
    "--name=alice",
    "--verbose",
    "-abc",
    "-o",
    "out.txt",
    "--",
    "--literal",
  ])
}

demo_parse()
