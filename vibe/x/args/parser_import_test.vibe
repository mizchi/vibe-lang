//# Imports

import ./parser.vibe {
  ParseResult,
  ArgList,
  ArgToken,
  parse_args,
}

//# Misc

test "simple use" {
  match parse_args([
    "--a=1"
  ]) {
    ParseOk(_) => assert(true),
    _ => assert(false)
  }
}
