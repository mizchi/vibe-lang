//# Imports

import .{
  Json, JsonSchema, validate, parse_args, ParseResult,
  ripple_new, ripple_record_eval, ripple_eval_count
}

//# Misc

test "x index exports jsonschema and args symbols" {
  assert(validate(SNumber, JNum(3.14)))
  match parse_args([
    "--flag",
  ]) {
    ParseOk(_) => assert(true),
    ParseErr(_) => assert(false),
  }
  let db1 = ripple_record_eval(ripple_new(), "a.vibe", "fp1", array_slice([""], 0, 0))
  assert(eq(ripple_eval_count(db1), 1))
}
