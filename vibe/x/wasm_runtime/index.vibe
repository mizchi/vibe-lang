//# Imports

export ./wasm_runtime.vibe {
  exec_body, exec_body_gc, make_memory, opcode_name, disassemble,
  exec_module, exec_module_with_args
}

//# Functions

export let version: String = "0.1.0"

//# Misc

export {
  exec_body, exec_body_gc, make_memory, opcode_name, disassemble,
  exec_module, exec_module_with_args, version
}
