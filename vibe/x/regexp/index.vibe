//# Imports

export ./regexp.vibe {
  Re, compile, find, find_all, find_match, is_match, replace_all, replace_first, split, test_match
}

//# Functions

export let version: String = "0.2.0"

//# Misc

export {
  Re, compile, test_match, find, find_match, find_all, is_match, replace_first, replace_all, split
}

export {
  Re, find, split, compile, version, find_all, is_match, find_match, test_match, replace_all, replace_first
}
