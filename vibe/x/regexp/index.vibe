//# Imports

export ./regexp.vibe {
  Re, compile, count, escape, find, find_all, find_match,
  is_match, replace_all, replace_first, split, test_match
}

//# Functions

export let version: String = "0.3.0"

//# Misc

export {
  Re, compile, count, escape, find, find_all, find_match,
  is_match, replace_all, replace_first, split, test_match
}

export {
  Re, compile, count, escape, find, find_all, find_match,
  is_match, replace_all, replace_first, split, test_match, version
}
