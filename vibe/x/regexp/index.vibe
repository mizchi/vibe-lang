//# Imports

export ./regexp.vibe {
  Re, Regex, Match,
  compile, compile_regex, count, escape, find, find_all, find_all_groups,
  find_match, get_capture, get_match_text, get_named_capture, has_match,
  is_match, match_groups, replace_all, replace_first, replace_with_groups,
  split, test_match
}

//# Functions

export let version: String = "0.4.0"

//# Misc

export {
  Re, Regex, Match,
  compile, compile_regex, count, escape, find, find_all, find_all_groups,
  find_match, get_capture, get_match_text, get_named_capture, has_match,
  is_match, match_groups, replace_all, replace_first, replace_with_groups,
  split, test_match
}

export {
  Re, Regex, Match,
  compile, compile_regex, count, escape, find, find_all, find_all_groups,
  find_match, get_capture, get_match_text, get_named_capture, has_match,
  is_match, match_groups, replace_all, replace_first, replace_with_groups,
  split, test_match, version
}
