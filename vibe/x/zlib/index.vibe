export let version = "0.0.1"

export ./adler32.vibe {
  adler32, adler32_init, adler32_update
}

export ./inflate.vibe {
  inflate
}

export ./deflate.vibe {
  deflate_stored
}

export ./zlib.vibe {
  zlib_compress_stored, zlib_decompress
}

export {
  adler32, adler32_init, adler32_update,
  inflate,
  deflate_stored,
  zlib_compress_stored, zlib_decompress
}
