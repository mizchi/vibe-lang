//# Imports

import .{
  get_or
}

//# Misc

test "collection index exports maps helpers" {
  let m: Map[String, Int] = map {
    "a": 1
  }
  assert(eq(get_or(m, "a", 0), 1))
  assert(eq(get_or(m, "z", 7), 7))
}
