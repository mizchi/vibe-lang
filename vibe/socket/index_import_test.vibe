//# Imports

import ./index.vibe {
  TcpEndpoint, TcpConnection,
  socket_connect, socket_read, socket_write, socket_close
}

//# Misc

test "socket index exports canonical aliases" {
  let _ = socket_connect
  let _ = socket_read
  let _ = socket_write
  let _ = socket_close
  assert(true)
}
