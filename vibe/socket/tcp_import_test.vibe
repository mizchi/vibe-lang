//# Imports

import ./tcp.vibe {
  tcp_connect, tcp_read, tcp_write, tcp_close
}

//# Misc

test "socket tcp module exports raw fd api" {
  let _ = tcp_connect
  let _ = tcp_read
  let _ = tcp_write
  let _ = tcp_close
  assert(true)
}
