//# Imports

import ./process_effect.vibe {
  Process
}

//# Misc

test "process mock exec" {
  let output = handle {
    perform Process::Exec("echo hello")
  } with Process {
    Exec(_cmd) => resume("hello\n");
    Env(_key) => resume("");
    Exit(_code) => resume(0)
  }
  assert(String::length(output) == 6)
}
