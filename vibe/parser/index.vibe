export let version = "0.0.1"

export ./lexer.vibe { lex, lex_ok }
export ./parser.vibe { parse, parse_ok, parse_program }
export ./printer.vibe { print_expr, print_stmt, print_program }
export ./token.vibe { Token }

// (A redundant bare `export { ... }` block used to follow the re-exports —
// #726/#742 disease: bare exports of names with no local def resolve to
// garbage under the FS merge. The re-export lines above are the API.)
