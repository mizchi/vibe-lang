# Accepted core-namespace codegen divergences between the linear and wasm-gc
# backends. check_codegen_parity.sh fails (exit 2) on any core divergence NOT
# listed here, so adding a builtin to one backend without the other is caught.
#
# Each line is a builtin name (Namespace::method). Blank lines and lines
# starting with `#` are ignored. Keep this list tight — entries here are
# parity gaps we have explicitly decided to tolerate for now.
#
# --- wasm-gc has, linear missing ---
# Native float math + Array::fold are implemented as wasm-gc codegen arms;
# the linear backend reaches the same behavior through the prelude / fallback
# path rather than a dedicated arm, so they show up as gc-only in the name
# scan. Tracked as a follow-up if linear ever needs native arms for these.
Array::fold
Double::ceil
Double::floor
Double::fract
Double::round
Double::sqrt
Double::trunc
Float::ceil
Float::floor
Float::fract
Float::round
Float::sqrt
Float::trunc
