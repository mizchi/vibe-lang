# examples/*.vibe that do NOT type-check, one repo-relative path per line.
# Checked by scripts/check_examples_typecheck.sh (wired into the compiler-gate
# CI job). This is a RATCHET: a listed file may fail, an unlisted file may not,
# and a listed file that starts PASSING is also an error, so the list cannot
# rot. Every entry is printed on every run.
#
# Adding an entry is debt. Fix the example, or fix the compiler, and delete the
# line. Each entry must name the issue that tracks it.
#
# (empty -- examples/syntax.vibe was the last entry and #1503 closed it)
