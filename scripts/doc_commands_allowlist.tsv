# Exceptions for scripts/check_doc_commands.sh: FILE <TAB> REFERENCE <TAB> REASON.
#
# Only two shapes belong here, and both must be visible to a reader of the
# document itself -- an allowlist entry is not a way to leave a command that
# looks current but is not.
#
#   1. a HISTORICAL command, where the prose says the thing is gone
#   2. a reference the READER is told to create locally
#
# Anything else is drift and should be fixed in the document.
docs/spec/rc-cutover-readiness.md	scripts/test_selfhost_cli_adapter.sh	historical measurement; the block's own comment says the script was pruned in #596 with no replacement
docs/wasm-opt-dogfood.md	VIBE_MINIFY_PASS	a local-only hook the reader is instructed to add, marked DO NOT COMMIT in the same block
