# Pinned shard slots for the gate self-test companions (check_gate_self_tests.sh,
# VIBE_GATE_SELF_TESTS_SHARD). Format: `<companion> <slot>`; a slot is reduced
# mod the shard count, so any count stays an exact partition. Unlisted
# companions are dealt round-robin. List only the heavy ones: without pins,
# round-robin put the two heaviest in one shard ([333, 277, 682]s).
#
# Seconds are one serial local run (2026-09-26, 56 companions, 1292s total).
# tests/gates/selftests/run.sh gives its named suites slots with this table in
# mind: slot 1 also gets checked-module parity + capability preflight (~80s),
# slot 2 gets checked-module cost + run_bounded (~190s).
check_compile_only_lanes_test.sh 0          # 493s
check_host_remove_parity_test.sh 1          # 234s
check_grep_memory_budget_test.sh 1          # 148s
test_component_lazy_dispatch_gate_test.sh 2 # 214s
check_grep_chunked_sweep_test.sh 2          # 66s
