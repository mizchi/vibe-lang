# Pinned shard slots for the gate self-test companions (check_gate_self_tests.sh,
# VIBE_GATE_SELF_TESTS_SHARD). Format: `<companion> <slot>`; a slot is reduced
# mod the shard count, so any count stays an exact partition. Unlisted
# companions (~70s between them) are dealt round-robin. List only the heavy ones.
#
# Seconds are CI's own, from the per-companion lines of run 36252326885
# (`[gate-self-tests] <s>s rc=<rc> <companion>`). A local serial run disagreed
# badly -- it had host-remove-parity at 234s (a viberun build CI restores from
# cache; 2s there) -- so re-balance from a CI log, not a local one.
# tests/gates/selftests/run.sh gives its named suites slots with this table in
# mind: slot 0 also gets capability preflight (~30s), slot 1 checked-module
# parity (~17s), slot 2 checked-module cost + run_bounded (~187s).
check_grep_driver_parity_test.sh 0           # 297s
check_compile_only_lanes_test.sh 1           # 164s
test_component_lazy_dispatch_gate_test.sh 1  # 148s
check_grep_memory_budget_test.sh 2           # 73s
check_grep_chunked_sweep_test.sh 2           # 48s
check_portable_boundary_test.sh 2            # 26s
