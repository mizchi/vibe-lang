# Tracked files whose name looks throwaway but is deliberate.
# Enforced by scripts/lint_tracked_experiment_names.sh.
#
# Format: <path> <category> [reason]
#   gate               a permanent gate/probe script a task or CI job runs
#   bench-fixture      a permanent measurement fixture
#   manual-experiment  kept on purpose, run by hand
#   archive-candidate  known debt: should be deleted or renamed
#
# Ratchet: an entry may be REMOVED (the file was deleted or renamed) but a new
# probe-named file has to be justified here first. The lint had NO allowlist
# file at all until #2252 -- so it failed on the tree, and nothing ran it, so
# nobody saw. Both halves are fixed: this file exists, and `release-check`
# invokes the lint.
scripts/cheatsheet_signature_probe.vibe gate corpus for check_cheatsheet_signatures.sh
scripts/test_async_string_lift_probe_gate.sh gate async string lift gate
scripts/test_gc_native_alloc_probe.sh gate wasm-gc native allocation gate
scripts/test_host_stream_value_probe_gate.sh gate host stream value gate
scripts/test_http_body_read_probe_gate.sh gate HTTP body read gate
scripts/test_http_body_stream_probe_gate.sh gate HTTP body stream gate
scripts/test_interleaved_tasks_probe_gate.sh gate interleaved task scheduling gate
scripts/test_wasi_cli_stdin_p3_probe_gate.sh gate WASI p3 stdin gate
scripts/tests/http_body_stream_probe_gate_test.sh gate self-test for the HTTP body stream gate
scripts/wasm_validate/exec_probe.vibex gate wasm-validate execution probe
scripts/wasm_validate/probe.vibex gate wasm-validate feature probe
scripts/wasm_validate/walk_probe.vibex gate wasm-validate module walk probe
