# Gate self-tests excluded from the "every companion must pass" rule.
#
# EMPTY, and pinned empty: `baseline_failing` in check_gate_self_tests.sh is
# the empty set, so an entry added here is REJECTED rather than honoured. A
# self-test that fails is a gate nobody has shown can fail; the repair is the
# test, not the exemption.
#
# Format (kept for the mechanism, not for current use):
#   <test basename> broken         fails wherever it runs
#   <test basename> env-dependent  fails according to what the runner has
#
# All five original entries were repaired in #2252. They had one root cause
# between them: each self-test inherited environment it did not control --
# `VIBE_REVIEW_LINT_GREP_BIN` from .claude/hooks/session-start.sh, and the
# presence of ripgrep, which the dev container has and CI does not.
