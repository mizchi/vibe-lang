# Gate self-tests excluded from the "every companion must pass" rule (#2252).
#
# Discovered the moment check_gate_self_tests.sh started actually RUNNING the
# companions it discovers -- until then nothing executed them.
#
# Format: <test basename> <kind>
#
#   broken         fails wherever it runs. Still EXECUTED: if it starts
#                  passing the exemption is stale and the gate says so, so a
#                  repair cannot leave the test permanently unrun.
#   env-dependent  passes or fails according to what the runner has. Executed
#                  too, but NOT treated as repaired when it passes -- "it
#                  worked here" is not evidence it works in CI, and removing
#                  it on that basis would turn CI red. The dependency itself
#                  is the bug to fix (see #2252).
#
# Baseline-pinned: entries may be REMOVED and never added.
lint_review_regressions_test.sh broken
lint_tracked_experiment_names_test.sh broken
check_lock_clean_test.sh env-dependent
lint_architecture_debt_test.sh env-dependent
lint_guest_profile_contract_test.sh env-dependent
