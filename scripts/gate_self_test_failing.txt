# Gate self-tests that exist but FAIL, pre-existing (#2252).
#
# Discovered the moment check_gate_self_tests.sh started actually RUNNING the
# companions it discovers -- until then nothing executed them, so a broken
# self-test sat green.
#
# WHICH ones fail depends on the environment, which is itself the finding:
#
#   with ripgrep (dev container):  lint_review_regressions_test.sh
#                                  lint_tracked_experiment_names_test.sh
#   without it (CI late lane):     check_lock_clean_test.sh
#                                  lint_architecture_debt_test.sh
#                                  lint_guest_profile_contract_test.sh
#
# The CI three fail with `rg: command not found` -- they hard-depend on
# ripgrep, which the late-lane runner does not have. They are not broken
# logic; they are unguarded on a tool. The other two fail with ripgrep
# present, so they are a separate problem.
#
# All five are excluded so the "run every companion" rule can land without
# turning CI red for reasons that predate it. Baseline-pinned like the
# allowlist: entries may be REMOVED (by fixing the test) and never added.
#
# Format: one test basename per line, relative to scripts/.
lint_review_regressions_test.sh
lint_tracked_experiment_names_test.sh
check_lock_clean_test.sh
lint_architecture_debt_test.sh
lint_guest_profile_contract_test.sh
