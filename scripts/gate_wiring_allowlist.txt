# Gates that deliberately run nowhere in CI. One per line: `<script.sh> <reason>`.
# A row here is a written admission, not a default -- and it goes stale loudly:
# a gate listed here that IS reached fails the check.
minify_gate.sh semantics gate for vibe-opt.wasm; needs that artifact, which no CI job produces. Invoked by pkf minify-gate / scripts/build_vibe_opt.sh.
test_parallel_warm_pool_gate.sh coordinator determinism probe; needs a release viberun and is not a WASI p3 vertical. Invoked by pkf test-warm-pool.
