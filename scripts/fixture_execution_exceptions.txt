# Fixtures that scripts/check_fixture_execution.sh must not flag (#1587).
#
# One repo-relative path per line, each preceded by the reason it is not run by
# any lane. "Not yet wired up" / "known broken" are not reasons -- a fixture in
# that state is either fixed or deleted, because an entry here is a promise
# that the file is deliberately inert. The check fails on entries whose file no
# longer exists, so this list cannot quietly rot.

# Sibling module imported by the import-syntax cases (import_extra_comma,
# import_malformed_separator, import_missing_from_keyword,
# parse_import_trailing_comma). It is the import TARGET, not a case of its own,
# so it has no verdict row -- it has to resolve for those parses to be the
# thing under test.
fixtures/typecheck/mod.vibe

# Sibling module imported by exception_kind_formal_alias_reject (#2985): the
# enum the case imports under the alias `T`. It is the import TARGET, not a
# case of its own, so it has no verdict row.
fixtures/typecheck/exception_kind_formal_alias_dep.vibe

# Sibling module imported by imported_row_poly_unhandled_effect_reject (#3014):
# the row-polymorphic helper the case imports. It is the import TARGET, not a
# case of its own, so it has no verdict row.
fixtures/typecheck/imported_row_poly_dep.vibe

# Sibling module imported by imported_show_no_seeded_impl_reject (#3022
# review): the user `trait Show` the case imports. It is the import TARGET,
# not a case of its own, so it has no verdict row.
fixtures/typecheck/imported_show_trait_dep.vibe

# Sibling module imported by imported_row_op_typo_reject (#3022 review): the
# function whose row names `Ask::Get`. It is the import TARGET, not a case of
# its own, so it has no verdict row.
fixtures/typecheck/imported_row_op_dep.vibe

# Sibling module imported by the foreign_type_name_* cases (#3026): the
# `struct Err` an importer may also declare for itself. It is the import
# TARGET, not a case of its own, so it has no verdict row.
fixtures/typecheck/foreign_type_name_dep.vibe

# Sibling module imported by foreign_type_name_private_kind_reject (#3026):
# a PRIVATE `Err` used as an exported function's exception kind. It is the
# import TARGET, not a case of its own, so it has no verdict row.
fixtures/typecheck/foreign_type_name_private_dep.vibe
