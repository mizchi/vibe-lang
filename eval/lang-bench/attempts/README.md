Round implementation artifacts land here as `<round>/<lang>/...` per each
language's `../langs/<lang>/README.md`. Not committed by default (see
`.gitignore` in this directory) — ephemeral working data, regenerated by
re-running a round.
