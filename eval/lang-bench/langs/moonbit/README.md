# mini-vcs — moonbit

**Toolchain not available in this sandbox** (`moon` not on `PATH`,
2026-07-22 — consistent with this repo's own #594 MoonBit-host retirement,
which removed the `moon` dependency from vibe's own build). Kept in
`langs/` for completeness since almide's upstream comparison and
`docs/pl-survey-2026-07.md` both treat MoonBit as a direct competitive
coordinate. Run wherever a MoonBit toolchain is installed and report
results back into `results/` manually, same as `langs/gleam/README.md`.
