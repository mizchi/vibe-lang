// ch03 "Names that are taken": most keywords escape with r#, `fn` does
// not, and "the diagnostic tells you which case you are in". Measure
// the `let fn` diagnostic: does it mention r# and that fn is the
// exception?
fn main with Console {
  let r#test = 1
  let fn = 2
  println("\{r#test}")
}
