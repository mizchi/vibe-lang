// Companion to p24_import_vibex.vibe (not itself a probe -- the
// runner's glob takes p*.vibe only). An executable root with an
// export: the export is accepted today despite ADR-0075 (#2229).
export fn helper() -> Int { 7 }
fn main with Console {
  println("dep")
}
