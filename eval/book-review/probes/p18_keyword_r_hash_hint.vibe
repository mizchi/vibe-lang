// ch03 (companion to p17): for an escapable keyword, does the
// diagnostic actually offer the r# spelling ("tells you which case you
// are in")?
fn main with Console {
  let match = 1
  println("\{match}")
}
