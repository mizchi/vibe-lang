// ch09 (round 2, R2-5): the split form's rule -- once `allows` is
// written, a capability may not sit in `with`. The book quotes the
// diagnostic; measure that it still matches. Note the real prefix is
// `error: line 1:1:` with NO file path -- the only diagnostic round 2
// met without one.
fn main() -> Int with Console allows Fs::read_file? {
  0
}
