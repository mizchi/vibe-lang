// ch03 (optional arguments): "`if flag` where `flag?: Bool` is a type
// error". Measure the diagnostic: does it explain that the body sees
// Option[Bool] and show the match?
fn f(flag?: Bool) -> Int {
  if flag {
    1
  } else {
    0
  }
}

fn main with Console {
  println("\{f(true)}")
}
