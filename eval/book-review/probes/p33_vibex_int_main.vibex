// ch01/ch09 neighborhood (round 2, R2-1): a .vibex entry must be
// `() -> Unit` -- found because ch09's split-form example writes
// `fn main() -> Int`, which can never run as a .vibex. Measured: the
// compile lane and `vibe check`/`vibe run` all answer
//   .vibex fn main must have type () -> Unit
// (no source position on any lane). The directives re-ask the check
// and run verbs; the compile lane below answers the same.
//! cli: check
//! cli: run
fn main() -> Int with () allows Console {
  0
}
