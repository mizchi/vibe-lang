// ch01: `vibe new` writes three files. Measured 2026-09-24 on stage2
// from 0f0ba364e: `.gitignore` (contains `.vibe/`), `index.vpkg`
// (`name = @local/proj`), and `main.vibex`. The older "two files,
// vibe.deps" note does not describe this compiler. The scaffold
// directive is the measurement; this file's own compile is incidental.
//! cli-scaffold: new
fn main allows Console {
  println("p30 descriptor")
}
