// ch18 (round 2, R2-6): the book's editor-query example. The chapter
// used to claim `vibe type-at hello.vibe 5 4` answers for `add`;
// measured, 5:4 sits on `main`. Re-measure against this file with:
//   vibe symbols  <this file>        -> add 12 <s> <e> / main 12 <s> <e>
//   vibe type-at  <this file> 8 4    -> (Int, Int) -> Int   (name `add`)
//   vibe type-at  <this file> 12 4   -> () -> () with Console (`main`)
// (line numbers here include this header; the book's file starts at
// `fn add`, where the pairs are 1:4 and 5:4.)
fn add(x: Int, y: Int) -> Int {
  x + y
}

fn main with Console {
  println("\{add(2, 40)}")
}
