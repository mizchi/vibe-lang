// ch01 (finding D11 neighborhood): "delete `with Console` and the
// program does not compile" is the book's opening claim. Measure the
// diagnostic a beginner sees first: is it located, and does the hint
// teach the current spelling (`with Console`) rather than a legacy one?
fn main {
  println("hello, vibe")
}
