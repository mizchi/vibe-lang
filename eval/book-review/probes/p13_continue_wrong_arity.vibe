// ch04: "If you give `continue` the wrong number of values the
// compiler says so and names both counts." Measure exactly that.
fn main with Console {
  let x = loop (i = 0, acc = 0) {
    if i > 3 {
      break acc
    }
    continue (i + 1)
  }
  println("\{x}")
}
