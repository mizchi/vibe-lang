// The compiler still accepts a legacy `Stdout` row, and that row does not
// reach `Console::write_stream`. Chapter 9 no longer quotes this diagnostic.
// Expected: rejected. The declared row stays `Stdout`. The hint pastes
// `allows Console::write_stream + Console`.
fn main allows Stdout {
  Console::write_stream("x")
}
