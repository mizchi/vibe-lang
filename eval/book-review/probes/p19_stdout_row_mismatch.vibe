// ch09 (round 2, R2-5): the book quotes the `with Stdout` mismatch
// diagnostic verbatim, including a hint that spells the repair with the
// legacy `Stdout` label the same chapter calls an older spelling
// (#1460). Measure that the quote still matches the compiler.
// Expected: rejected at 2:3 with "effect row mismatch for 'main':
// missing { Console::write_stream }" and the hint
// "add 'with Console::write_stream + Stdout' to 'main'".
fn main with Stdout {
  Console::write_stream("x")
}
