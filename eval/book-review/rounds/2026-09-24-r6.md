# book-review round 6 — 2026-09-24

Probe round. Stage2 is
`_build/selfhost/generations/array-capacity-2026-09-20_0f0ba364e/stage2.wasm`,
built from commit `0f0ba364e`. The manifest says `source.dirty` because
the book and eval edits were uncommitted. `lib/@vibe`, `lib/@vibex` and
`bootstrap/seed` were clean, so `BOOK_REVIEW_STAGE2` was passed
explicitly. 38 probes, no harness error. Log kept only as the run's
stdout; the grades are in `scores/chapters/*/2026-09-24-r2.json` and
`scores/passes/2026-09-24-r2.json`.

## What the probes changed in the book

- Chapter 1. Deleting `allows Console` is diagnosed as `allows Stdout`.
  The chapter now says so, and still tells the reader to write `Console`.
  `vibe new` writes `main.vibex`, `index.vpkg` and `.gitignore`. The
  probe header that said two files and `vibe.deps` was the stale half.
- Chapter 4. `for-in` was stated as collecting for every iterand. It is
  an `Array`. A pull iterator in that position is the pitfall chapter's
  error. Both languages say that now.
- Chapter 17. The quoted hint said `with Console + Exception[TaskError]`.
  With `VIBE_UNSTABLE=1` the compiler says `allows`. Without the flag
  the import is refused first and the hint is never reached. The chapter
  now says both. `perform?` (p21) compiles and prints `not granted`;
  the probe header that still predicted a rejection was updated.

## Left as a compiler fact, not a book edit

p35. A `#pkg:` pin at the top of a `.vibex` makes `vibe check` clean and
`vibe run` answer `unknown # directive: pkg`. The book puts pins in
`index.vpkg`, so the sentence was not rewritten to hide this. The two
lanes disagree.

p04. `vibe check` names `derive(Show)` and anchors `main`, not the
interpolation. Chapter 5 says the missing renderer is a compile error.
It does not claim the position is the brace. Score on that probe is 3.7,
not 5.

p27. ``no impl `Eq` for `Array[Int]` `` matches chapter 16 and has no
source position.
