# book-review round — 2026-09-25

The missing-Show interpolation was anchored on the expression's first
byte. That byte is also the argument's offset, so two call nodes shared
one typed-lowering key and CI refused the tree. The call now carries
the backslash of `\{`.

`vibe check` on the probe is `line 13:12`, still on the interpolation,
and the message still names `derive(Show)`. The compile sidecar has no
line. Erased-formal refusals gained the same location; the refusal gate
strips a `line L:C:` header before it checks that the edit leads.
