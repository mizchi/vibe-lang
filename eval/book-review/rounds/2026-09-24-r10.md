# book-review round 10 — 2026-09-24

Interpolating a struct with no `derive(Show)` named the edit, then
anchored the enclosing `main`. The `__to_string` call the parser builds
for `\{...}` carried no offset, so the only marker was `[@fn=]`.

The call now carries the first byte of the expression, the byte right
after `\{`. `vibe check` on the probe is
`line 13:14: cannot interpolate a value of type `Point` ... add `derive(Show)` ...`.
That column is `p`. The compile sidecar still has no line: this throw
is stripped there rather than resolved. A dependency file is located
by requiring those two preceding bytes to be `\{` in that file, so an
in-range offset does not get blamed on the entry.

Pinned by `lib/@vibe/compiler/tests/diagnostic_anchor_test.vibe` and
the missing-Show rows in `lib/@vibe/compiler/tests/cli_support_test.vibe`.
Chapter 5's off-path score moves from 3.7 to 5. Pass record:
`scores/passes/2026-09-24-r6.json` (off-path mean 4.75 over 11/22).
Chapter text unchanged.
