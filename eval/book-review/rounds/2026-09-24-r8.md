# book-review round 8 — 2026-09-24

p34's `vibe check` answer now has a position. The message was
`import kind mismatch: `HashMap` is a type alias, not a struct` with
no line. It now carries the import path, and `locate_import_kind_msg`
anchors on `import <path>` (a comment that merely quotes the package
does not win) and then the name.

Re-measured on a rebuilt stage2:
`error: line 13:28:` at `HashMap` in
`import @vibe/core { struct HashMap }`. The compile sidecar is still
positionless: that lane refuses the text heuristic on purpose. The
book's verb for this question is `vibe check`.

Pinned by
`lib/@vibe/compiler/tests/checker_unused_import_and_discard_test.vibe`.
Chapter 11's off-path score moves from 3.7 to 5. Pass record:
`scores/passes/2026-09-24-r4.json` (off-path mean 4.6 over 11 chapters).
