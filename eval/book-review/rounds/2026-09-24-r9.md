# book-review round 9 — 2026-09-24

`if flag` when `flag?: Bool` used to say only
`if condition must be Bool, got Option[Bool]`. Chapter 3 says to match
it. The diagnostic now adds `match it, or pass a Bool`, and it sits on
`flag`.

Re-measured on a rebuilt stage2, both lanes:
`line 7:6: if condition must be Bool, got Option[Bool]; match it, or pass a Bool`.

Pinned by `lib/@vibe/compiler/tests/diagnostic_anchor_test.vibe`.
Chapter 3's off-path score moves from 4.6 to 5. Pass record:
`scores/passes/2026-09-24-r5.json`.
