# book-review round 7 — 2026-09-24

p35 closed. `vibe run`'s preflight parsed a `.vibex` raw, so a
`require ... = #pkg:` header was an `unknown # directive: pkg`. `vibe
check` and the compile lane already blanked that header (#2227).
`parse_user_entry_file` now blanks it too, which is also the parse
`vibe serve` and `vibe build --wit` use.

Re-measured on a stage2 rebuilt from this tree:
`vibe run` prints `pin head parsed` and exits 0, `vibe check` is empty.
Pinned by `lib/@vibe/cli/dispatch_test.vibe` ("user run plan accepts a
require pin head on a vibex"), 103 tests in that file, on the previous
stage2 compiling the new source, and by the probe on the rebuilt stage2.

Chapter 11's off-path score moves from 3.0 to 3.7. The remaining point
is p34, which still has no source position. Pass record:
`scores/passes/2026-09-24-r3.json`.
