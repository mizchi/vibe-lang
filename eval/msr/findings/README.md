Round findings land here as `<date>-r<N>.md` — per-task failure analysis
(compile error class, semantic misunderstanding, misleading diagnostic,
etc.) for any task whose `modified` stage failed. Major findings get filed
as GitHub issues (same operating rule as `eval/lang-review/findings/`).
Empty until the first round runs.
