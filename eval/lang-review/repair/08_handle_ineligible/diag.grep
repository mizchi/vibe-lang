handle of effect 'Ask' cannot be compiled here
this handle cannot see what one call in its body performs
(here: the call to 'bump')
declare 'bump' as a top-level `fn`
give the binding or parameter it arrives through an effect row (`with Ask`)
move its `let` inside the handled body
Moving the `handle` into the function that performs works too
