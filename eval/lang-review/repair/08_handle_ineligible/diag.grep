handle of effect 'Ask' cannot be compiled here
the handled body calls 'bump'
a local binding whose type carries no effect row
Give the binding an explicit type carrying the row
or make it a top-level `fn`
