effect Ask {
  Once() -> Int
}
fn ask_once() -> Int with Ask {
  perform Ask::Once()
}
fn main allows Console {
  let bump = (x: Int) -> Int { ((y: Int) -> Int { y })(x) }
  let v = handle { bump(ask_once()) } with {
    Ask::Once() => resume(41)
  }
  Console::write_stream("\{v}\n")
}
