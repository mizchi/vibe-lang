effect Ask {
  Once() -> Int
}
fn ask_once() -> Int with Ask {
  perform Ask::Once()
}
fn main with Stdout {
  let bump = (x: Int) -> Int { x + 1 }
  let v = handle { bump(ask_once()) } with {
    Ask::Once() => resume(41)
  }
  Stdout::write_stream("\{v}\n")
}
