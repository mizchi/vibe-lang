import ../../../lib/@vibe/prelude { stdout_write }
import ../../../lib/@vibe/core { gcd, encode }

let main: () -> Unit with Stdout = () -> {
  stdout_write("gcd(12,18)=\{gcd(12, 18)}\n")
  stdout_write("base64(hello)=\{encode("hello")}\n")
}
