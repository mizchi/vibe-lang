//# Imports

import ./maps.vibe {
  get_or, has_key
}

//# Misc

test "maps facade re-exports map helpers" {
  let m: Map[String, Int] = Map::from_pairs([("a", 1)])
  assert(has_key(m, "a"))
  assert(eq(get_or(m, "a", 0), 1))
}
