//# Imports

import @vibe/compiler/core {
  Type
}

//# Functions

/// All Bytes and SIMD scanner signatures are checker-visible registry rows.
/// Keep this fallback hook so the lookup chain remains structurally stable.
export fn lookup_bytes(_name: String) -> Option[Type] {
  None
}
