//# Imports

import @vibe/compiler/core {
  Type
}

//# Functions

/// FrozenArray signatures are checker-visible registry rows (#1593).
export fn lookup_frozen_array(_name: String) -> Option[Type] {
  None
}
