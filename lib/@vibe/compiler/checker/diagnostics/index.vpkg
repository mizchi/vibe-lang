name = @vibe/compiler/checker/diagnostics
version = 0.0.1
description =
  #|Encode checker diagnostic anchors and recover declaration anchors.
deps = {}

generated_hash =

fn off_marker(off: Int) -> String
fn off_marker_len(off: Int, len: Int) -> String
fn attach_unlocated_decl_name_marker(errors: Array[String], from: Int, name: String) -> Unit
