//# Imports

import @vibe/compiler/core {
  Type
}

//# Functions

/// ArrayBuilder signatures are checker-visible registry rows (#1593).
export fn lookup_array_builder(_name: String) -> Option[Type] {
  None
}

/// StringBuilder signatures are checker-visible registry rows (#1593).
export fn lookup_string_builder(_name: String) -> Option[Type] {
  None
}
