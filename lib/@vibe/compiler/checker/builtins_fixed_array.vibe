//# Imports

import @vibe/compiler/core {
  Type
}

//# Functions

/// FixedArray signatures are checker-visible registry rows (#1593).
export fn lookup_fixed_array(_name: String) -> Option[Type] {
  None
}
