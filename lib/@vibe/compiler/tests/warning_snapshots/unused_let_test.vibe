import ./helpers.vibe {
  scope_warnings
}

test "unused let names the unused binding" {
  inspect(scope_warnings("let unused = 1\nlet used = 2\nused"), "variable 'unused' is defined but never used")
}
