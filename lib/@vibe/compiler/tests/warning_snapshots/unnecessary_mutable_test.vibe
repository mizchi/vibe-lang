import ./helpers.vibe {
  scope_warnings
}

test "used mutable binding that is never assigned" {
  inspect(scope_warnings("let mut value = 1\nvalue"), "mutable variable 'value' is never reassigned, use 'let' instead")
}
