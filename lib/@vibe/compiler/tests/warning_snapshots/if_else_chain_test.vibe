import ./helpers.vibe {
  diagnostics
}

test "three branch if chain is accepted" {
  inspect(diagnostics("fn main() -> Int { let x = 1\nif x == 1 { 1 } else if x == 2 { 2 } else if x == 3 { 3 } else { 0 } }"), "")
}
