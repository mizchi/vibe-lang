import ./helpers.vibe {
  scope_warnings
}

test "raw contextual keyword bindings are used" {
  inspect(scope_warnings("let r#map = 1\nlet r#loop = 2\nlet r#derive = 3\nr#map + r#loop + r#derive"), "")
}
