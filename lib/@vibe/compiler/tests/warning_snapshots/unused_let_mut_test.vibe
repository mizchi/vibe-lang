import ./helpers.vibe {
  scope_warnings
}

test "unused mutable binding reports both unused and never reassigned" {
  inspect(scope_warnings("let mut never_set = 1\nlet x = 2\nx"), "variable 'never_set' is defined but never used\nmutable variable 'never_set' is never reassigned, use 'let' instead")
}
