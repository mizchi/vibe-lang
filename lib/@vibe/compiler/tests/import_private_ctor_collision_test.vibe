import ../_import_private_ctor_collision_a.vibe { pick_left }
import ../_import_private_ctor_collision_b.vibe { pick_right }

test "dependency private enum constructor names do not collide" {
  assert_eq(pick_left() + pick_right(), 30)
}
