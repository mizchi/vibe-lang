//# Imports

import ../_import_alias_codegen_support.vibe {
  answer as aliased_answer
}

//# Misc

test "compiled backend resolves imported support function alias" {
  assert(eq(aliased_answer(), 42))
}
