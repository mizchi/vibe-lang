import ../_import_alias_collision_a.vibe { answer as answer_a }
import ../_import_alias_collision_b.vibe { answer as answer_b }

test "import aliases keep same original names distinct in compiled tests" {
  assert_eq(answer_a() + answer_b(), 3)
}
