//# Imports

import @vibe/compiler/syntax {
  lex, print_expr
}

import ../parser.vibe {
  parse
}

//# Functions

export fn parse_print(src: String) -> String with Exception {
  let tokens = lex(src)
  let expr = parse(tokens)
  print_expr(expr)
}
