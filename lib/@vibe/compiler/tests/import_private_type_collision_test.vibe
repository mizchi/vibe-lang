import ../_import_private_type_collision_a.vibe {
  answer_a
}
import ../_import_private_type_collision_b.vibe {
  answer_b
}

test "dependency private enum names do not collide" {
  assert_eq(answer_a() + answer_b(), 3)
}
