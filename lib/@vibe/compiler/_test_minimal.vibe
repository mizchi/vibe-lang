//# Imports

import ./core {
  Expr,
  Pat,
  TypeExpr
}

//# Functions

fn test_fn(e: Expr) -> String {
  match e {
    EInt(n) => "int",
    EFloat(n) => "float",
    EString(s) => s,
    EBool(b) => "bool",
    EIdent(name, _) => name,
    EUnit => "()",
    _ => "other"
  }
}
