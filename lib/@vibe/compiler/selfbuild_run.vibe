//# Imports

import @vibe/compiler/codegen {
  compile_wasi_module
}

import @vibe/compiler/loader {
  collect_merged_stmts
}

import ./compiler_sources_bundle.vibe {
  compiler_sources
}

//# Functions

fn selfbuild_main() -> Int with Exception {
  let sources = compiler_sources()
  let merged = collect_merged_stmts(sources)
  let bytes = compile_wasi_module(merged, "selfbuild_compile_stage2")
  Bytes::length(bytes)
}
