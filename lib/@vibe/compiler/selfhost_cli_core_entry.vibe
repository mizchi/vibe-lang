//# Imports

import ../cli/selfhost.vibe {
  selfhost_cli_main
}

//# Effects

effect Profiler {
  NowUs -> Int
}

//# Functions

export fn cli_main() -> Int with { Error, Fs, Env, Profiler } {
  selfhost_cli_main()
}
