name = @vibe/compiler/fmt/rewrite
version = 0.0.1
description =
  #|Canonical token rewrites before layout. Each rewrite preserves the established pass order.
deps = {
  @vibe/compiler/fmt/lexer : 0.0.1
  @vibe/compiler/fmt/token_stream : 0.0.1
}

generated_hash =

fn rewrite_tokens(toks: Array[LTok]) -> Array[LTok]
