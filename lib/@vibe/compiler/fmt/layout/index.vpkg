name = @vibe/compiler/fmt/layout
version = 0.0.1
description =
  #|Token layout, width decisions, spacing, escaping, and indentation.
deps = {
  @vibe/compiler/fmt/lexer : 0.0.1
  @vibe/compiler/fmt/token_stream : 0.0.1
}

generated_hash =

fn format_tokens(toks: Array[LTok], indent_size: Int) -> String
