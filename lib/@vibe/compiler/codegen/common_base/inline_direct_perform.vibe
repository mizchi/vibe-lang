// Shared codegen entry points for independently owned effect lowering passes.

export ../../lowering/effects/await {
  await_poll_pass
}

export ../../lowering/effects/direct {
  inline_direct_performs
}

export ../../lowering/effects/discard {
  forin_discard_pass
}

export ../../lowering/effects/evidence {
  edp_bind_census, evidence_dict_pass, evidence_module_facts, verify_pure_builtin_name_agreement
}

export ../../lowering/effects/optional {
  collect_optional_perform_source_names, lower_optional_performs, lower_optional_performs_with_source_names, optional_perform_artifact_resolution
}

export ../../lowering/effects/suspend {
  suspend_cps_pass
}
