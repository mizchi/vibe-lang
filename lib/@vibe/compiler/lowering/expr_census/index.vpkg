name = @vibe/compiler/lowering/expr_census
version = 0.0.1
description =
  #|Read-only questions about an AST fragment, answered in one allocation-free walk.
deps = {
  @vibe/compiler/core : 0.0.1
}

generated_hash =

fn collect_pat_binders(p: Pat, out: Array[String]) -> Unit
fn strip_param_tilde(n: String) -> String
fn strip_label_tilde(n: String) -> String
fn census_set_call_names(names: Array[String]) -> Unit
fn census_scan(expr: Expr, mode: Int, name: String) -> Bool
fn census_pat_binds(pat: Pat, name: String) -> Bool
fn census_mentions(expr: Expr, name: String) -> Bool
fn census_fresh_name(base: String, value: Expr, content: Expr) -> String
fn census_inspect_bits(expr: Expr) -> Int
