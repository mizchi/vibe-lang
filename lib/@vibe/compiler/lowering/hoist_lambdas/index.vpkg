name = @vibe/compiler/lowering/hoist_lambdas
version = 0.0.1
description =
  #|Hoist non-capturing effectful local lambdas to top-level definitions (#786).
deps = {
  @vibe/compiler/core : 0.0.1
  @vibe/compiler/lowering/expr_census : 0.0.1
  @vibe/core : 0.2.0
}

generated_hash =

fn hoist_local_lambdas(stmts: Array[Stmt], own_end: Int) -> Int
fn hoist_lambdas_set_module_tag(tag: String) -> Unit
fn hoisted_lambda_owner_of_stmt(stmt: Stmt, idx: Int) -> String
fn hoisted_lambda_owner_key(name: String) -> Option[String]
