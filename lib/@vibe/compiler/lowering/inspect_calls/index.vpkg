name = @vibe/compiler/lowering/inspect_calls
version = 0.0.1
description =
  #|Expand `inspect(value, content)` to its stringify / print / assert form before checking (#1571).
deps = {
  @vibe/compiler/core : 0.0.1
  @vibe/compiler/lowering/expr_census : 0.0.1
}

generated_hash =

fn desugar_inspect_calls(stmts: Array[Stmt]) -> Unit
fn desugar_inspect_calls_reporting_change(stmts: Array[Stmt]) -> Bool
