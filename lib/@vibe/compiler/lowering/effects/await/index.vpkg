name = @vibe/compiler/lowering/effects/await
version = 0.0.1
description =
  #|Lower await calls into polling and ready-value expressions.
deps = {
  @vibe/compiler/core : 0.0.1
  @vibe/core : 0.2.0
}

generated_hash =

fn await_poll_pass(stmts: Array[Stmt], host_future: Bool, waiter_hooks: Bool) -> Unit
