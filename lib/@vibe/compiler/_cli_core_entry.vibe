//# Imports

import ../cli/dispatch.vibe {
  selfhost_cli_main
}

//# Effects

effect Profiler {
  NowUs -> Int
}

//# Functions

export fn cli_main() -> Int with Exception + Fs + Env + Profiler + Stdin + Stdout + Stderr {
  selfhost_cli_main()
}
