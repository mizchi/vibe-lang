//# Imports

import ./core {
  Expr,
  Pat,
  TypeExpr
}

//# Functions

let test_fn: (Array[(Pat, Expr)]) -> Array[String] = (arms) -> {
  for arm in arms {
    let (pat, expr) = arm
    "item"
  }
}
