name = @vibe/compiler/module_graph
version = 0.0.1
description =
  #|@vibe/compiler/module_graph — coordinator-side import-DAG ranking
deps = {}

generated_hash =

// @vibe/compiler/module_graph — coordinator-side import-DAG ranking
// (#1239 step 4A, #1353). Pure apart from Exception -- dependency
// resolution needs Fs, ordering does not. Relocated from @vibe/graph
// because every consumer is compiler runtime/typecheck; there is no
// remaining compiler-independent DAG here.

fn plan_module_order(edges: Array[(String, Array[String])], root: String) -> (Array[String], Array[Int]) with Exception
