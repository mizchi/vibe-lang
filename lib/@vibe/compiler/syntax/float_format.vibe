//# Imports

// #753 (ADR-0065 Phase 3): the lexer/parser/printer live in the
// @vibe/parser contract package; this module is a re-export shim so the
// compiler's existing import spellings keep working.
export @vibe/parser {
  double_to_string_compiler
}
