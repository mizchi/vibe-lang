name = @vibe/time
version = 0.0.1
description =
  #|@vibe/time — wall-clock helpers (ADR-0070 Phase 3, #897). now_utc_iso /
deps = {
  @vibe/process : 0.0.1
}

generated_hash =

// @vibe/time — wall-clock helpers (ADR-0070 Phase 3, #897). now_utc_iso /
// now_local_date shell out via sh_lines (Process effect, checker builtin —
// see checker/builtins_system.vibe, no import needed); sleep_ms carries the
// Async checker-builtin effect (checker/builtins_async.vibe). time.vibe's
// `first_line` helper is a private (non-exported) implementation detail and
// is intentionally not part of this contract.

fn now_utc_iso() -> String with Process
fn now_local_date() -> String with Process
fn sleep_ms(ms: Int) -> Unit with Async
