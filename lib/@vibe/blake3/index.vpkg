name = @vibe/blake3
version = 0.0.1
description =
  #|@vibe/blake3 — BLAKE3 hash (256-bit, hex digest)。
deps = {}

generated_hash =

// @vibe/blake3 — BLAKE3 (spec-complete: chunk/tree hashing, official test
// vectors in blake3_test.vibe)。出力は 256-bit digest の little-endian hex。

fn blake3(msg: String) -> String
fn blake3_bytes(data: Bytes) -> String

// Linear-backend SIMD kernels live in nested @vibe/blake3/simd so this
// scalar contract is not pulled into the compiler flatten with inline wasm.
