name = @vibe/blake3/simd
version = 0.0.1
description =
  #|Linear-backend SIMD BLAKE3. Nested so the scalar @vibe/blake3 contract
  #|does not auto-discover inline-wasm kernels into the compiler flatten.
deps = {}

generated_hash =

fn blake3_simd(msg: String) -> String
fn blake3_bytes_simd(data: Bytes) -> String
