name = @vibe/semver
version = 0.0.1
description =
  #|@vibe/semver — SemVer の parse/比較/bump。
deps = {}

generated_hash =

// @vibe/semver — SemVer の parse/比較/bump。
// Semver は bodyless type 宣言 (#739 の透過 re-export — 定義は impl 側)。
// 契約に enum 本体を置く形は「impl 自身が ctor を構築/match する」パッケージ
// では check 順序 (impl が facade の型定義より先に check される) に当たる
// ため、この形が正解。Semver::* は名前空間 fn 宣言 (#752)。

type Semver

fn parse(s: String) -> Semver
fn compare(a: Semver, b: Semver) -> Int
fn eq(a: Semver, b: Semver) -> Bool
fn gt(a: Semver, b: Semver) -> Bool
fn lt(a: Semver, b: Semver) -> Bool
fn gte(a: Semver, b: Semver) -> Bool
fn lte(a: Semver, b: Semver) -> Bool
fn bump_major(v: Semver) -> Semver
fn bump_minor(v: Semver) -> Semver
fn bump_patch(v: Semver) -> Semver
fn Semver::major(v: Semver) -> Int
fn Semver::minor(v: Semver) -> Int
fn Semver::patch(v: Semver) -> Int
fn Semver::to_string(v: Semver) -> String
fn Semver::prerelease(v: Semver) -> String

