// @vibe/wit_runtime implementation stub. The package's public surface is the single
// transparent type definition in index.vpkg (`Result[T, E]`), following the
// @vibe/ast precedent (#753): the definition itself IS the contract, so it
// lives in the facade. This file exists as the explicit impl import target,
// which suppresses sibling auto-discovery (result_test.vibe beside the
// contract must not join the facade).
