//# Imports

import ./http_effect.vibe {
  HttpServer
}

//# Misc

test "imported effect" {
  let fd = handle {
    perform HttpServer::Listen(8080)
  } with HttpServer {
    Listen(_port) => resume(42)
  }
  assert(fd == 42)
}
