//# Imports

import ./result.vibe {
  Err, Ok, Result
}

//# Misc

test "result_type_import_and_method_style" {
  assert(Ok(1).is_ok())
  assert(Err("x").is_err())
  assert(eq(Err("x").unwrap_or(7), 7))
  assert(Ok(2).is_ok())
  assert(eq(Err("x").or(10), 10))
}
