//# Imports

import ./tcp.vibe {
  tcp_close, tcp_connect, tcp_read, tcp_write
}

//# Misc

test "socket tcp module exports raw fd api" {
  let _ = tcp_connect
  let _ = tcp_read
  let _ = tcp_write
  let _ = tcp_close
  assert(true)
}
