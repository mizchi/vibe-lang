//# vibe/collection — maps / derive(Hash) machinery ONLY. list and set moved
//# to the @vibe/core contract package (lib/@vibe/core, ADR-0063); maps/map
//# follow once the contract grammar can declare traits (Hash).

export ./maps.vibe {
  get, get_by, get_or, get_or_by, get_or_else, get_or_else_by, has_by, has_key
}

//# Functions

export let version: String = "0.0.1"

//# Misc

export {
  get, get_or, get_or_else, has_key, get_by, has_by, get_or_by, get_or_else_by, version
}
