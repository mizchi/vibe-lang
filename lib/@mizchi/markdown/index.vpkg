name = @mizchi/markdown
version = 0.2.0
description =
  #|@mizchi/markdown — markdown parse/serialize + code block 抽出。
deps = {
  @vibe/builtin : 0.0.1
}

generated_hash =

// @mizchi/markdown — markdown parse/serialize + code block 抽出。
// コンパイラ非関連モジュールは実ユーザー scope に置く (ADR-0065)。

type Document
type Block
type CodeBlock
type BulletMarker
type FenceMarker
type HeadingStyle

fn parse(source: String) -> Document
fn serialize(doc: Document) -> String
fn extract_code_blocks(source: String) -> Array[CodeBlock]
fn extract_code_blocks_by_lang(source: String, lang: String) -> Array[String]
fn extract_vibe_source(source: String) -> String

