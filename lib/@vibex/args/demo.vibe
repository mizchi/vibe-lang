//# Imports

import ./parser.vibe {
  ArgList, ArgToken, ParseResult, parse_args
}

//# Functions

export fn demo_parse() -> ParseResult {
  parse_args([
    "--name=alice",
    "--verbose",
    "-abc",
    "-o",
    "out.txt",
    "--",
    "--literal",
  ])
}
