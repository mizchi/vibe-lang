version 0.0.1

// @vibex/jsonschema — 自前 Json 表現に対する JSON Schema 検証
// (ADR-0065 実験 scope)。

import ./types.vibe {}
import ./validate.vibe {}

type Json
type JsonSchema

fn validate(schema: JsonSchema, value: Json) -> Bool
fn JsonSchema::validate(schema: JsonSchema, value: Json) -> Bool

let version: String
