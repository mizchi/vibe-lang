// @vibex/base64 — base64 / URL-safe base64 の encode/decode。
// @vibex は実験 scope (ADR-0065): @vibe への昇格候補。

import ./base64.vibe {}

fn decode(input: String) -> String
fn encode(input: String) -> String
fn encode_url_safe(input: String) -> String

let version: String
