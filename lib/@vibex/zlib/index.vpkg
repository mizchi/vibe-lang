name = @vibex/zlib
version = 0.0.1
description =
  #|@vibex/zlib — inflate / stored deflate / adler32 / bitstream
deps = {}

generated_hash =

// @vibex/zlib — inflate / stored deflate / adler32 / bitstream
// (ADR-0065 実験 scope)。BitReader/BitWriter は mut field 構造体 (ADR-0052)。

type BitReader
type BitWriter
type HuffTable

fn adler32(buf: Bytes) -> Int
fn adler32_init() -> Int
fn adler32_update(state: Int, buf: Bytes, off: Int, count: Int) -> Int
fn new_reader(buf: Bytes) -> BitReader
fn read_bits(r: BitReader, n: Int) -> Int with Error
fn read_byte(r: BitReader) -> Int with Error
fn align_to_byte(r: BitReader) -> Unit
fn new_writer() -> BitWriter
fn write_bits(w: BitWriter, value: Int, n: Int) -> Unit
fn write_byte(w: BitWriter, b: Int) -> Unit
fn flush_to_byte(w: BitWriter) -> Unit
fn writer_bytes(w: BitWriter) -> Bytes
fn build_table(lengths: Array[Int]) -> HuffTable with Error
fn decode_symbol(t: HuffTable, r: BitReader) -> Int with Error
fn inflate(data: Bytes) -> Bytes with Error
fn deflate_stored(data: Bytes) -> Bytes
fn zlib_compress_stored(data: Bytes) -> Bytes
fn zlib_decompress(data: Bytes) -> Bytes with Error

