// @vibex/rlm — Llm effect 越しの補完/実行ヘルパ (ADR-0065 実験 scope)。

import ./rlm.vibe {}

fn run(task: String, prompt: String) -> String with { Llm }
fn complete(system: String, user: String) -> String with { Llm }
fn finalize(result: String) -> String

let version: String
