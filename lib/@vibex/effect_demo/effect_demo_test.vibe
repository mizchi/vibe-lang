//# Imports

import ./effect_demo.vibe {
  empty_array
}

//# Misc

test "empty_array creates typed empty array" {
  let xs: Array[Int] = empty_array()
  assert(Array::length(xs) == 0)
  Array::push(xs, 42)
  assert(Array::length(xs) == 1)
  assert(Array::get(xs, 0) == 42)
}
