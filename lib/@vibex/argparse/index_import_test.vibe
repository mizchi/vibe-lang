//# Imports

import .{
  ArgList, ArgToken, ParseResult, parse_args
}

//# Misc

test "x/args index exports parser symbols" {
  match parse_args(["--a=1",]) {
    ParseOk(_) => assert(true),
    ParseErr(_) => assert(false),
  }
}
