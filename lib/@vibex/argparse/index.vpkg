name = @vibex/argparse
version = 0.0.1
description =
  #|@vibex/argparse — CLI 引数の tokenize/parse (ADR-0065 実験 scope)。
deps = {}

generated_hash =

// @vibex/argparse — CLI 引数の tokenize/parse (ADR-0065 実験 scope)。

type ArgToken
type ArgList
type ParseResult

fn parse_args(args: Array[String]) -> ParseResult
fn demo_parse() -> ParseResult

