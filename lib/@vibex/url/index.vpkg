name = @vibex/url
version = 0.0.1
description =
  #|@vibex/url — URL parse/encode + URLPattern (ADR-0065 実験 scope)。
deps = {
  @vibe/scan : 0.0.1
  @vibex/regexp : 0.0.1
}

generated_hash =

// @vibex/url — URL parse/encode + URLPattern (ADR-0065 実験 scope)。
// pattern.vibe は @vibex/regexp に依存する (契約 import)。

type Url
type UrlPattern
type UrlPatternMatch
type UrlPatternPart

fn parse(raw: String) -> Url
fn encode(s: String) -> String
fn decode(s: String) -> String
fn encode_query(pairs: Array[(String, String)]) -> String
fn decode_query(qs: String) -> Array[(String, String)]
fn Url::scheme(u: Url) -> String
fn Url::host(u: Url) -> String
fn Url::port(u: Url) -> Int
fn Url::path(u: Url) -> String
fn Url::query(u: Url) -> String
fn Url::fragment(u: Url) -> String
fn compile_pattern(raw: String) -> UrlPattern
fn exec_pattern(pattern: UrlPattern, input: String) -> UrlPatternMatch
fn test_pattern(pattern: UrlPattern, input: String) -> Bool
fn pattern_has_match(m: UrlPatternMatch) -> Bool
fn get_param(m: UrlPatternMatch, name: String) -> String

