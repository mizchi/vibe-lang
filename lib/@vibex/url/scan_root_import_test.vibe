//# Imports

import @vibex/scan {
  type Scanner
}

//# Misc

test "root import exposes scan helpers under Scanner namespace" {
  let scan: Scanner = Scanner::new("left:right")
  assert(Scanner::remaining(scan) == "left:right")
}
