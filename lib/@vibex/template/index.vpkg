version 0.0.1

// @vibex/template — `{key}` プレースホルダの文字列テンプレート (ADR-0065 実験 scope)。

fn render(template: String, vars: Array[(String, String)]) -> String
fn render_map(template: String, vars: Map[String, String]) -> String
fn has_vars(template: String) -> Bool
fn extract_keys(template: String) -> Array[String]

