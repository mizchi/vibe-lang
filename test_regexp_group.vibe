import ./vibe/x/regexp/regexp.vibe { compile, test_match }

test "simple group" {
  let re = compile("(ab)+")
  assert(test_match(re, "ab"))
  assert(test_match(re, "abab"))
}

test "nested group" {
  let re = compile("((ab)+c)+")
  assert(test_match(re, "abc"))
}
