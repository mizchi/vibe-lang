let rec build: (Int, ArrayBuilder[Int]) -> ArrayBuilder[Int] = (n, b) -> {
  if eq(n, 0) {
    b
  } else {
    {
      ArrayBuilder::push(b, n)
      build(sub(n, 1), b)
    }
  }
}

let result = {
  let b = ArrayBuilder::new()
  let out = build(200, b)
  ArrayBuilder::freeze(out)
}
let main = () -> Array[Int] {
  result
}
