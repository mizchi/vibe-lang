// #1056 binary-size regression bench: recursive function calls.
// See bench/binary_size/README.md.

fn fib(n: Int) -> Int {
  if n < 2 {
    n
  } else {
    fib(n - 1) + fib(n - 2)
  }
}

let main = () -> Int {
  fib(20)
}
