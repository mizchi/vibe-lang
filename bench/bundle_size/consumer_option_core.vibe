import ./prelude/option.vibe { is_some, unwrap_or, map_or }

let a = is_some(Some(1))
let b = unwrap_or(None, 7)
let c = map_or(Some(5), 0, (x) -> x * 2)

let main = () -> String {
  if a && eq(b, 7) && eq(c, 10) { "ok" } else { "ng" }
}
