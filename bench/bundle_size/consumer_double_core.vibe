import ./prelude/double.vibe { abs, max, lerp }

let a = abs(-3.5)
let b = max(2.0, 7.0)
let c = lerp(0.0, 10.0, 0.25)

let main = () -> String {
  if a == 3.5 && b == 7.0 && c == 2.5 { "ok" } else { "ng" }
}
