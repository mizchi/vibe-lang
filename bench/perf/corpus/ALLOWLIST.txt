# Bench files that may read a LIVE source path as their corpus (#2865).
#
# Ratchet: rows may be REMOVED, never added without a reason on the same line.
# A bench whose input is a file that PRs edit is not measuring its subject --
# that is the whole of #2865 -- so a new row is a written admission, not a
# formality.
#
# <path><TAB><reason>
lib/@vibe/compiler/loader_hotspot_probe.vibe	permanent: the loader's subject IS the live tree. It resolves a real import closure from a real entry path, and a frozen copy has no import context to resolve.
lib/@vibe/compiler/fmt_bench.vibe	debt, #2878: same defect, not pinned here. Its three corpora are also re-export shims, so `format_large_file` measures 662 bytes and allocates LESS than `format_small_file`.
