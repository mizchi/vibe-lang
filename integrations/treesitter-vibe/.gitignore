node_modules/
build/
*.wasm

# ...except the stale-artifact fixtures, which are deliberately committed.
# `git add -A` silently skipped one under the rule above, so the self-test that
# needs it passed locally (the file was on disk, just untracked) and failed on a
# fresh clone. See test/fixtures/README.md.
!test/fixtures/*.wasm
