declare export let keep: () -> Int
declare export let run: () -> Int
let id = (x: Int) -> Int {
  x
}
keep = () -> Int {
  id(1)
}
let answer: Int = 42
run = () -> Int {
  answer
}
export {
  run, keep
}

