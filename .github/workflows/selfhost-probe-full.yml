name: Selfhost Probe Full

on:
  workflow_dispatch:

jobs:
  selfhost-probe-full:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    env:
      VIBE_SELFHOST_PROBE_FULL: "1"
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install MoonBit CLI
        run: |
          curl -fsSL https://cli.moonbitlang.com/install/unix.sh | bash
          echo "$HOME/.moon/bin" >> "$GITHUB_PATH"

      - name: Moon update
        run: moon update

      - name: Run full selfhost probe (vibe integration index 44)
        run: moon test -p tests -f vibe_integration_test.mbt --target js --warn-list '-29' --index 44

      - name: Install wasmtime
        run: |
          curl -fsSL https://wasmtime.dev/install.sh | bash
          echo "$HOME/.wasmtime/bin" >> "$GITHUB_PATH"

      - name: Install wasm-tools
        run: |
          curl -LO https://github.com/bytecodealliance/wasm-tools/releases/download/v1.245.1/wasm-tools-1.245.1-x86_64-linux.tar.gz
          tar xzf wasm-tools-1.245.1-x86_64-linux.tar.gz
          sudo mv wasm-tools-1.245.1-x86_64-linux/wasm-tools /usr/local/bin/

      - name: Selfhost linked build probes
        continue-on-error: true
        run: scripts/test_selfhost_probes.sh
