fn mk2(a: Int, b: Int) -> Array[Int] {
  [a, b]
}

fn main with Console {
  // no outer shadow at all
  let xs = []
  let ys = []
  Array::push(xs, mk2(1, 2))
  Array::push(ys, mk2(1, 2))
  println("no shadow, direct push = \{xs == ys}")
}
