test "match tuple pattern" {
  let compare = (a: Int, b: Int) -> Int {
    match (a > b, a == b) {
      (true, _) => 1,
      (_, true) => 0,
      _ => -1
    }
  }
  inspect(compare(5, 3) + compare(3, 3) + compare(1, 5), "0")
}
