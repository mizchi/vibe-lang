let rec sum_to = (n: Int) -> Int {
  if n <= 0 { 0 }
  else { n + sum_to(n - 1) }
}
let rec fib = (n: Int) -> Int {
  if n <= 1 { n }
  else { fib(n - 1) + fib(n - 2) }
}
let r1 = sum_to(10)
let r2 = fib(7)
r1 + r2

__DATA__
{"last": "68"}
