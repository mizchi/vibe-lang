// Parameterized loop: factorial of 5

test "loop param factorial" {
  inspect(loop (n = 5, acc = 1) {
    if n <= 1 {
      break acc
    } else {
      continue (n - 1, acc * n)
    }
  }, "120")
}
