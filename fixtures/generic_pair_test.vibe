// Generic pair functions

test "generic pair" {
  let pair = [A, B](a: A, b: B) -> (A, B) {
    (a, b)
  }
  let fst = [A, B](p: (A, B)) -> A {
    p.0
  }
  let snd = [A, B](p: (A, B)) -> B {
    p.1
  }
  let p = pair(1, "hello")
  inspect(fst(p), "1")
}
