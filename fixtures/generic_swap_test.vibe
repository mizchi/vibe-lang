// #1967: was fixtures/generic_swap.vibe + __DATA__.last ("two", 1).

fn swap[A, B](a: A, b: B) -> (B, A) {
  (b, a)
}

test "generic swap swaps the pair" {
  let pair = swap(1, "two")
  match pair {
    (s, n) => {
      inspect(s, "two")
      inspect(n, "1")
    }
  }
}
