let classify = (n: Int) -> Int {
  match n {
    x if x < 0 => -1,
    0 => 0,
    x if x > 100 => 2,
    _ => 1
  }
}

classify(-5) + classify(0) + classify(50) + classify(200)

__DATA__
{"last": "2"}
