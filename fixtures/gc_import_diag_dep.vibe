// Helper for fixtures/gc_import_diag_use.vibe (#1976). Kept out of the
// `*_test.vibe` glob on purpose: unit_test_runner discovers those, and this
// pair exists to be COMPILED AND REJECTED, not to pass.
export fn gc_import_diag_helper(n: Int) -> Int {
  n * 2
}
