import @vibe/prelude {  String  }

let a = "" |> String::is_empty
let b = "hello world" |> String::contains("lo wo")
let c = "foo bar foo" |> String::replace_all("foo", "x")

if a && b && String::equals(c, "x bar x") { "ok" } else { "ng" }

__DATA__
{"last":"\"ok\""}
