// CPS accumulate: product instead of sum
effect Yield { Yield(Int) -> Unit }

let product = handle {
  perform Yield::Yield(2)
  perform Yield::Yield(3)
  perform Yield::Yield(5)
  1
} {
  Yield::Yield(v, k) => v * k(0)
}
// 2 * (3 * (5 * 1)) = 30
product

__DATA__
{"last": "30"}
