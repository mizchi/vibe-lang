// Higher-order function return type mismatch
let apply = [A, B](f: (A) -> B, x: A) -> B { f(x) }
let to_string = (x: Int) -> String { "num" }

fn main() -> Unit {
  let _ = apply(to_string, 42) + 1
}
