test "array filter" {
  let arr = [1, 2, 3, 4, 5, 6]
  let b = ArrayBuilder::new()
  let _ = (for x in arr {
    if x % 2 == 0 {
      ArrayBuilder::push(b, x)
    }
  })
  let evens = ArrayBuilder::freeze(b)
  inspect(Array::length(evens), "3")
}
