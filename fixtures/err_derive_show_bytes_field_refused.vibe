// #3092: a `derive (Show)` struct with a `Bytes` field printed the field's
// address. Refused, naming the edit.
struct D {
  b: Bytes
} derive (Show)

export let _start: () -> Int = () -> {
  let d = D::{ b: Bytes::from_array([1, 2]) }
  let s = "d=\{d}"
  String::length(s)
}
