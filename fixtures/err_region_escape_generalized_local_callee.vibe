test "generalized local callees preserve capture provenance" {
  let escaped = region r {
    let values = MutList::empty(r)
    let read = (unused) -> Array[Int] {
      let _ = unused
      MutList::to_array(values)
    }
    () -> Array[Int] {
      read(0)
    }
  }
  let _ = escaped()
}
