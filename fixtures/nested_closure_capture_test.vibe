test "nested closure capture" {
  let make_adder = (x: Int) -> (Int) -> Int {
    (y: Int) -> Int {
      x + y
    }
  }
  let add5 = make_adder(5)
  let add10 = make_adder(10)
  inspect(add5(3) + add10(7), "25")
}
