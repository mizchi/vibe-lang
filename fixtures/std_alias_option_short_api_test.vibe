import @vibe/builtin {
  is_some, map_or, unwrap_or
}

test "std alias option short api" {
  let a = Some(1) |> is_some
  let b = None |> unwrap_or(7)
  let c = Some("abc") |> map_or(0, (s) -> String::length(s))
  inspect(if a && eq(b, 7) && eq(c, 3) {
    "ok"
  } else {
    "ng"
  }, "ok")
}
