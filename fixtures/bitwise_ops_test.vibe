test "bitwise ops" {
  let a = 0xFF
  let b = 0x0F
  let and_result = a&b
  let or_result = a|0x100
  let xor_result = a^b
  let shift_left = 1<<8
  inspect(and_result + (or_result - 0x100) + xor_result + (shift_left - 256), "510")
}
