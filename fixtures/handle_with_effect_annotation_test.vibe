test "handle with effect annotation" {
  let risky = () -> Int with Exception {
    throw("fail")
    0
  }
  let safe = () -> Int {
    handle {
      risky()
    } with { Exception::Throw(_) => 99 }
  }
  inspect(safe(), "99")
}
