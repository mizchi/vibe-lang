// Scenario 4: Effect op returning Unit used in expression position

effect Logger {
  Log(String) -> Unit
}

test "effect perform unit expr position" {
  let result = handle {
    let x = perform Logger::Log("hello")
    42
  } with { Logger::Log(_msg) => resume(0) }
  inspect(result, "42")
}
