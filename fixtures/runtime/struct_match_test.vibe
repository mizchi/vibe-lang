//# Types

struct Point {
  x: Int; y: Int
}

//# Functions

let p = Point::{
  x: 3, y: 4
}

let sum = match p {
  Point::{
    x, y
  } => add(x, y),
  _ => 0
}

//# Misc

test "struct_match" {
  assert(eq(sum, 7))
}
