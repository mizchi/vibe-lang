export let main = () -> Unit with Process + Console {
  let h = vibe_sh_capture_raw("echo hi")
  Console::write_stream("handle=\{h}\n")
  let ec = vibe_sh_capture_exit_code_raw(h)
  Console::write_stream("exit_code=\{ec}\n")
}
