export let main = () -> Unit with Process + Stdout {
  let h = vibe_sh_capture_raw("echo hi")
  Stdout::write_stream("handle=\{h}\n")
  let ec = vibe_sh_capture_exit_code_raw(h)
  Stdout::write_stream("exit_code=\{ec}\n")
}
