// #865 manual/e2e check fixture: returns a specific non-zero Int so
// `bash scripts/vibe_run.sh fixtures/runtime/exit_code_check.vibe; echo $?`
// can be verified to propagate it as the real OS exit status instead of
// always exiting 0 regardless of what `main` returned (see the #865 report).
export let main = () -> Int {
  17
}
