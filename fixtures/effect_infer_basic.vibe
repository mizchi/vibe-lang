// Effect inference: with ... omitted, inferred from perform
effect Greet { Hello -> String }

// No explicit with — Greet inferred from perform inside handle
let result = handle {
  let msg = perform Greet::Hello
  String::length(msg)
} { Greet::Hello => resume("world") }
result

__DATA__
{"last": "5"}
