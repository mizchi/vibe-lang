// Array of generic functions - works with monomorphization

test "err type generic in array" {
  let identity = [T](x: T) -> T {
    x
  }
  let arr = [identity, identity]
  inspect(arr[0](42), "42")
}
