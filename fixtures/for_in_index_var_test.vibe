test "for in index var" {
  let _start = () -> {
    let items = ["a", "b", "c"]
    let mut result = 0
    for i, item in items {
      result = result + i + String::length(item)
    }
    result
  }
  inspect(_start(), "6")
}
