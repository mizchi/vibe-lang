trait LocalEq
impl LocalEq for Int
impl LocalEq for Int

// A declaration-only file compiles to nothing, so the check under test never
// runs. An empty `main` gives it a program to check.
fn main() -> Unit { }
