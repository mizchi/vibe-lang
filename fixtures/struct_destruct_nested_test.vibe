// Multiple struct destructuring via let

struct Vec2 { x: Int; y: Int }

test "struct destruct nested" {
  let a = Vec2::{ x: 3, y: 4 }
  let b = Vec2::{ x: 10, y: 20 }
  let Vec2::{ x, y } = a
  let sum_a = x + y
  let Vec2::{ x, y } = b
  let sum_b = x + y
  inspect(sum_a + sum_b, "37")
}
