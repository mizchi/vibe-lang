// ADR-0071 (#755) step 5: consumes fixtures/contract_effectset_signature_alias/,
// proving check_contract accepts a contract signature spelled with an
// effectset alias against an implementation spelled with the literal
// operations it expands to. Compiled directly by compiler_gate.sh, same as
// fixtures/contract_effectset_vpkg_main.vibe.
import ./contract_effectset_signature_alias { read_one }

export let main = () -> Int with Ask + Ask::Get {
  handle {
    read_one()
  } with Ask {
    Get => resume(42)
  }
}
