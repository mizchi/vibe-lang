//# Pin the `!=` spelling for an initially untyped `let mut` array as well.

export fn _start() -> Int {
  let mut left = [
  ]
  Array::push(left, 1)
  let mut right = [
  ]
  Array::push(right, 2)
  if left != right {
    1
  } else {
    0
  }
}
