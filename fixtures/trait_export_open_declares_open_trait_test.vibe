// #1967: was fixtures/trait_export_open_declares_open_trait.vibe +
// __DATA__.last "true".

export open trait LocalEq
impl LocalEq for String

fn same[T: LocalEq](a: T, b: T) -> Bool {
  true
}

test "open LocalEq same is true" {
  inspect(same("ok", "ok"), "true")
}
