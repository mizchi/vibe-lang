// ADR-0068 `Send`: not a user trait — `impl Send for X` must be rejected
// (a user impl could smuggle a mutable-interior type across a task
// boundary and break the deep-copy snapshot contract).
struct Pt { x: Int; y: Int }

impl Send for Pt
