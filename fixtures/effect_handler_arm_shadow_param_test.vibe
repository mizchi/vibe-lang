// Scenario 15: Handler arm uses effect op parameter

effect Kv {
  Get(String) -> Int
}

test "effect handler arm shadow param" {
  let result = handle {
    perform Kv::Get("key")
  } with { Kv::Get(key) => resume(String::length(key)) }
  inspect(result, "3")
}
