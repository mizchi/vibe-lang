// Generic map over optional values using match

test "generic option" {
  let maybe_double = (x: Int, has_value: Bool) -> Int {
    match has_value {
      true => x * 2
      _ => 0
    }
  }
  inspect(maybe_double(21, true), "42")
}
