test "for in flat sum" {
  let _start = () -> {
    let rows = [[1, 2], [3, 4], [5, 6]]
    let mut sum = 0
    for row in rows {
      for val in row {
        sum = sum + val
      }
    }
    sum
  }
  inspect(_start(), "21")
}
