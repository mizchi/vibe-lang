// #1967: was fixtures/trait_export_sealed_trait_local_impl.vibe +
// __DATA__.last "true".

export trait LocalEq
impl LocalEq for Int

fn same[T: LocalEq](a: T, b: T) -> Bool {
  true
}

test "sealed LocalEq local impl same is true" {
  inspect(same(1, 1), "true")
}
