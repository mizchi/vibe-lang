let might_fail = () -> Int {
  let x = handle {
    throw("inner")
    0
  } with Error {
    Throw(_) => 1
  }
  throw("outer")
  x
}

__DATA__
{"error_contains": "effect requirements not satisfied"}
