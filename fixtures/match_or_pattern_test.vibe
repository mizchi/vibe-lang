enum Color {
  Red;
  Green;
  Blue
}

test "match or pattern" {
  let is_warm = (c: Color) -> Bool {
    match c {
      Red|Green => true,
      Blue => false
    }
  }
  let a = is_warm(Red)
  let b = is_warm(Blue)
  inspect(if a && !b {
    1
  } else {
    0
  }, "1")
}
