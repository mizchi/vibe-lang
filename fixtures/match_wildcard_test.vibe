enum Shape {
  Circle(Int);
  Rect(Int, Int);
  Point
}

test "match wildcard" {
  let area = (s: Shape) -> Int {
    match s {
      Circle(r) => r * r * 3,
      Rect(w, h) => w * h,
      _ => 0
    }
  }
  inspect(area(Circle(5)) + area(Rect(3, 4)) + area(Point), "87")
}
