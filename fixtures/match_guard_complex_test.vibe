test "match guard complex" {
  let classify = (x: Int, y: Int) -> Int {
    match (x, y) {
      (a, b) if a > 0 && b > 0 => 1,
      (a, _) if a > 0 => 2,
      (_, b) if b > 0 => 3,
      _ => 0
    }
  }
  inspect(classify(1, 1) + classify(1, -1) + classify(-1, 1) + classify(-1, -1), "6")
}
