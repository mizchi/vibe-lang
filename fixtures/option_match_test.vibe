test "option match" {
  let find_positive = (n: Int) -> Option[Int] {
    if n > 0 {
      Some(n)
    } else {
      None
    }
  }
  let result = match find_positive(42) {
    Some(x) => x,
    None => -1
  }
  inspect(result, "42")
}
