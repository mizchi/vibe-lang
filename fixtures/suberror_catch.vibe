suberror MathError {
  DivByZero
}

let safe_div = (a: Int, b: Int) -> Int with Exception {
  if b == 0 { throw(DivByZero) }
  a / b
}

let result = handle {
  safe_div(10, 0)
} with Exception {
  Throw(_) => -1
}
result

__DATA__
{"last": "-1"}
