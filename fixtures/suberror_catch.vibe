suberror MathError {
  DivByZero
}

let safe_div = (a: Int, b: Int) -> Int with { Error } {
  if b == 0 { throw(DivByZero) }
  a / b
}

let result = handle {
  safe_div(10, 0)
} {
  Error(_) => -1
}
result

__DATA__
{"last": "-1"}
