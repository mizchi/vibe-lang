//# The `!=` spelling must use the same fail-closed equality guard as `==`.

export fn _start() -> Int {
  let left = [
  ]
  Array::push(left, 1)
  let right = [
  ]
  Array::push(right, 2)
  if left != right {
    1
  } else {
    0
  }
}
