enum Expr {
  Num(Int);
  Add(Expr, Expr)
}

test "match nested ctor" {
  let rec eval_expr = (e: Expr) -> Int {
    match e {
      Num(n) => n,
      Add(a, b) => eval_expr(a) + eval_expr(b)
    }
  }
  inspect(eval_expr(Add(Num(1), Add(Num(2), Num(3)))), "6")
}
