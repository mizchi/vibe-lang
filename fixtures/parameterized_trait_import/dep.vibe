export trait Value[A] {
  value(Self) -> A
}

export struct Box {
  value: Int
}

impl Value[Int] for Box {
  value(self: Box) -> Int {
    self.value
  }
}

export fn read_value[T: Value[Int]](item: T) -> Int {
  T::value(item)
}
