// Function expects 2 args, passed to HOF expecting 1 arg
let apply = [A, B](f: (A) -> B, x: A) -> B { f(x) }
let add = (a: Int, b: Int) -> Int { a + b }

fn main() -> Unit {
  let _ = apply(add, 42)
}
