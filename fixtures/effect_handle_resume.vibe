// Effect handler with resume: inline perform in handle body
effect Console {
  ReadLine -> String
}

// Handle provides mock implementation (tail-resumptive inline)
let result = handle {
  let name = perform Console::ReadLine
  String::length(name)
} {
  Console::ReadLine => resume("Alice")
}
result

__DATA__
{"error_contains": "String::length"}
