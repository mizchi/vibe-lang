// Flip function arguments

test "generic flip" {
  let flip = [A, B, C](f: (A, B) -> C) -> (B, A) -> C {
    (b: B, a: A) -> C {
      f(a, b)
    }
  }
  let sub = (a: Int, b: Int) -> Int {
    a - b
  }
  let flipped = flip(sub)
  inspect(flipped(3, 10), "7")
}
