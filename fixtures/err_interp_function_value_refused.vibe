// #3075: a bare function value is refused the same way as one inside a
// container: it has no text form, and rendering it printed an address.
export let _start: () -> Int = () -> {
  let f = (x: Int) -> x + 1
  let s = "f=\{f}"
  String::length(s)
}
