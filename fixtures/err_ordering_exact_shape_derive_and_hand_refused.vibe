// #3042 (Codex on #3063): listing the derive does not license a hand-written
// replacement -- the importer is checked against the prelude's
// `compare -> Ordering`, the dependency links its own `compare -> Int`
// (tests/gates/early/run.sh).
import ./modules/ordering_exact_shape_derive_and_hand_dep.vibe { sign_of }

fn main() -> Unit {
  let _ = Ordering::compare(sign_of(1), sign_of(0 - 1))
}
