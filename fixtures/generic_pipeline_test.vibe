// Pipeline of generic functions

test "generic pipeline" {
  let identity = [T](x: T) -> T {
    x
  }
  let double = (x: Int) -> Int {
    x * 2
  }
  let inc = (x: Int) -> Int {
    x + 1
  }
  inspect(identity(double(identity(inc(identity(10))))), "22")
}
