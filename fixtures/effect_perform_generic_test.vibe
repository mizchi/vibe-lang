effect State[S] {
  Get -> S;
  Put(S) -> Unit
}

test "effect perform generic" {
  let counter = () -> Int with State {
    let n = perform State::Get
    perform State::Put(n + 1)
    n
  }
  inspect(0, "0")
}
