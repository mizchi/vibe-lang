// #2913: the bare `iter_get`, same shape as the bare `iter_length`. The
// protocol is spelled qualified (`C::iter_get(xs, i)`).
fn main() -> Unit allows Console {
  println(Int::to_string(iter_get([1, 2, 3], 0)))
}
