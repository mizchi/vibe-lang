test "bytes ops" {
  let b = Bytes::new()
  let _ = (Bytes::push(b, 65))
  let _ = (Bytes::push(b, 66))
  let _ = (Bytes::push(b, 67))
  let arr = Bytes::to_array(b)
  inspect(Array::length(arr), "3")
}
