struct StructAliasWrap {
  callback: () -> Array[Int]
}

fn build_struct_alias() -> StructAliasWrap {
  region r {
    let values = MutList::empty(r)
    let wrapped = StructAliasWrap::{
      callback: () -> Array[Int] {
        MutList::to_array(values)
      }
    }
    wrapped
  }
}
