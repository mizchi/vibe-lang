test "map set has" {
  let m = MapBuilder::new()
  let _ = (MapBuilder::set(m, "key", 42))
  let frozen = MapBuilder::freeze(m)
  inspect(if Map::has_key(frozen, "key") {
    Map::get(frozen, "key")
  } else {
    0
  }, "42")
}
