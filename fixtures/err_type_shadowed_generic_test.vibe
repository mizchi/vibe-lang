// Type parameter shadowing in nested function

test "err type shadowed generic" {
  let outer = [T](x: T) -> T {
    let inner = [T](y: T) -> T {
      y
    }
    inner(x)
  }
  inspect(outer(42), "42")
}
