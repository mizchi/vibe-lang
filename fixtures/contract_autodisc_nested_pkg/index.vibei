// #847: like contract_autodisc_pkg (#730), but one implementation file lives
// in a nested subdirectory (`sub/`) that is NOT itself an index.vibe(i)
// boundary. Auto-discovery must recurse into it, not just readdir the
// contract's own top-level directory.

fn triple(x: Int) -> Int
fn shout(s: String) -> String
fn double(x: Int) -> Int
