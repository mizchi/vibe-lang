// One implementation file lives below `sub/`. Only direct production siblings
// are implicit roots, so impl_a.vibe brings it into the graph explicitly.

fn triple(x: Int) -> Int
fn shout(s: String) -> String
fn double(x: Int) -> Int
