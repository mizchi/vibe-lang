// #847: nested implementation file in a non-boundary subdirectory (no
// index.vibe(i) of its own) — auto-discovery must recurse into `sub/` to
// find this, not just readdir the contract's own top-level directory.

export fn double(x: Int) -> Int { x * 2 }
