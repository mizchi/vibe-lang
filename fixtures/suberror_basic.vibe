suberror AppError {
  NotFound(String);
  InvalidInput(String)
}

let validate = (n: Int) -> Int with Error {
  if n < 0 {
    throw(InvalidInput("negative"))
  }
  n * 2
}

let result = handle {
  validate(5)
} with Error {
  Throw(_) => -1
}
result

__DATA__
{"last": "10"}
