// ADR-0090 MutBytes: the region token is unforgeable, same rule and same
// checker predicate as MutList::empty (err_region_token_forged.vibe).
//   error_contains: "region token"
fn probe() -> Int {
  let b = MutBytes::empty(0)
  MutBytes::push(b, 1)
  0
}
