enum List {
  Nil;
  Cons(Int, List)
}

let rec length = (lst: List) -> Int {
  match lst {
    Nil => 0,
    Cons(_, rest) => 1 + length(rest)
  }
}

length(Cons(1, Cons(2, Cons(3, Nil))))

__DATA__
{"last": "3"}
