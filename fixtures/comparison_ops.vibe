let _start = () -> {
  let a = 5
  let b = 10
  let eq = a == b
  let neq = a != b
  let lt = a < b
  let lte = a <= b
  let gt = a > b
  let gte = a >= b

  let mut score = 0
  if !eq { score = score + 1 }
  if neq { score = score + 1 }
  if lt { score = score + 1 }
  if lte { score = score + 1 }
  if !gt { score = score + 1 }
  if !gte { score = score + 1 }
  score
}
_start()

__DATA__
{"last": "6"}
