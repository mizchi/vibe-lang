test "labeled arg optional" {
  let greet = (name~: String, excited~: Bool) -> Int {
    if excited {
      String::length(name) * 2
    } else {
      String::length(name)
    }
  }
  inspect(greet(name = "hi", excited = true), "4")
}
