// Scenario 4: Effect op returning Unit used in expression position
effect Logger {
  Log(String) -> Unit
}

let result = handle {
  let x = perform Logger::Log("hello")
  42
} with {
  Logger::Log(_msg) => resume(0)
}
result

__DATA__
{"last": "42"}
