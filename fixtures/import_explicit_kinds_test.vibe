import ./modules/import_explicit_kind_lib.vibe {
  type IntBox, add_eq, trait LocalEq
}

test "import explicit kinds" {
  let box_id = (x: IntBox) -> IntBox {
    x
  }
  inspect(add_eq(box_id(1), 1), "1")
}
