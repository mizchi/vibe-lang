// for-in with mutation accumulation

test "for in accumulate" {
  let doubled = for x in [1, 2, 3] {
    x * 2
  }
  let sum = {
    let mut s = 0; for x in doubled {
      s = s + x
    }; s
  }
  inspect(sum, "12")
}
