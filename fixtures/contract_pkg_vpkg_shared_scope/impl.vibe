// #897: uses `seed` (from @vibe/random) WITHOUT importing it itself --
// inherited from index.vpkg's directory-shared `import @vibe/random { seed }`.
export fn wrapped_seed(x: Int) -> Int {
  seed(x)
}
