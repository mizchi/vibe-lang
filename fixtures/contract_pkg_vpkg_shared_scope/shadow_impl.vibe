// #897 shadowing test: this file defines its OWN `seed`, which must win over
// the directory-shared import inherited from index.vpkg -- a sibling's own
// explicit declaration always shadows an inherited one.
fn seed(x: Int) -> Int {
  x + 1000
}

export fn shadow_seed(x: Int) -> Int {
  seed(x)
}
