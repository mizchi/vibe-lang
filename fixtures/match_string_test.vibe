test "match string" {
  let classify = (s: String) -> Int {
    match s {
      "yes" => 1,
      "no" => 0,
      _ => -1
    }
  }
  inspect(classify("yes") + classify("no") + classify("maybe"), "0")
}
