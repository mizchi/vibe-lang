package example:http-lite@1.0.0;

interface types {
  record response {
    status: s32,
    body: stream<u8>,
  }
}

interface client {
  use types.{response};

  fetch-a: async func() -> response;
  pending-count: async func() -> s64;
}
