// Underscore-prefixed -- must be EXCLUDED from implicit membership.
// Exports a name not declared in index.vpkg; if wrongly swept in, this
// causes a conformance violation.
export fn scratch_only(x: Int) -> Int { x }
