// #2913: the bare `iter_require`, same shape. It was typed
// `(Unknown) -> Unknown` and emitted by nothing at all -- not even a stale
// desugar target, so there is no spelling that made it work.
fn main() -> Unit allows Stdout {
  iter_require([1, 2, 3])
  println("unreachable")
}
