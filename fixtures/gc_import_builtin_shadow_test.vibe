// #2376: enabling multi-file wasm-gc compilation must not let a literal
// builtin fast path hijack an imported user function with the same local name.

import ./gc_import_builtin_shadow_dep.vibe {
  to_string
}

test "imported user function wins over the GC builtin fast path" {
  assert_eq(to_string(42), "custom")
}
