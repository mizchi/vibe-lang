// String::join: join array of strings with separator
let parts = ["hello", "world", "foo"]
let joined = String::join(parts, ", ")
let split_back = String::split(joined, ", ")
let rejoined = String::join(split_back, "-")
rejoined

__DATA__
{"last": "\"hello-world-foo\""}
