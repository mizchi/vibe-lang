// string_join: join array of strings with separator
let parts = ["hello", "world", "foo"]
let joined = string_join(parts, ", ")
let split_back = string_split(joined, ", ")
let rejoined = string_join(split_back, "-")
rejoined

__DATA__
{"last": "\"hello-world-foo\""}
