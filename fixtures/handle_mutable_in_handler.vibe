let _start = () -> {
  let mut count = 0
  let result = handle {
    count = count + 1
    throw("err")
    0
  } with Exception {
    Throw(_) => count
  }
  result
}
_start()

__DATA__
{"last": "0"}
