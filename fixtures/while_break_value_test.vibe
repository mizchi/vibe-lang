// while loop with bare break

test "while break value" {
  let _start = () -> {
    let mut i = 0
    let mut sum = 0
    while true {
      if i >= 5 {
        break
      } else {
      }
      sum = sum + i
      i = i + 1
    }
    sum
  }
  inspect(_start(), "10")
}
