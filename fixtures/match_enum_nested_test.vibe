enum Tree {
  Leaf(Int);
  Node(Tree, Tree)
}

test "match enum nested" {
  let rec sum = (t: Tree) -> Int {
    match t {
      Leaf(n) => n,
      Node(l, r) => sum(l) + sum(r)
    }
  }
  inspect(sum(Node(Node(Leaf(1), Leaf(2)), Leaf(3))), "6")
}
