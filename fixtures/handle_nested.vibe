let outer = handle {
  let inner = handle {
    throw("inner")
    0
  } with Exception {
    Throw(msg) => String::length(msg)
  }
  inner + 100
} with Exception {
  Throw(_) => -1
}
outer

__DATA__
{"last": "105"}
