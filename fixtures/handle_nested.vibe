let outer = handle {
  let inner = handle {
    throw("inner")
    0
  } with Error {
    Throw(msg) => String::length(msg)
  }
  inner + 100
} with Error {
  Throw(_) => -1
}
outer

__DATA__
{"last": "105"}
