// The negative bound is -2^62, so -(2^62+1) does not fit. The lexer sees the
// magnitude, which is already past the 2^62-1 literal bound (#1877).
fn main() -> Unit {
  let _ = -4611686018427387905
}
