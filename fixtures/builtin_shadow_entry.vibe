// #3132 review: `--entry StringBuilder::new` names this program's own
// function. The compiler renames a definition spelled like a builtin it
// calls internally; the entry must still resolve to it and be exported under
// the name written here. Built by tests/gates/mid/run.sh (40f-b5).

fn StringBuilder::new() -> Int with Console {
  println("user entry ran")
  42
}
