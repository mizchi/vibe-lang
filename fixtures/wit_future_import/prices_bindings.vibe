// GENERATED from prices.wit by `from_wit_future_imports` (@vibex/wasm_wit_parser).
// Do not edit: from_wit_test.vibe checks that this file is exactly that
// derivation, so the import set here comes from the WIT, not from hand.

/// `example:prices/api@1.0.0#get-price`, imported as a host future (#2064).
export fn get_price() -> Future[Int] {
  host_future_named("example:prices/api@1.0.0#get-price")
}

/// `example:prices/api@1.0.0#get-tax`, imported as a host future (#2064).
export fn get_tax() -> Future[Int] {
  host_future_named("example:prices/api@1.0.0#get-tax")
}
