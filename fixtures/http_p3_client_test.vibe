// Phase 3: HTTP client test for WASI P3 client adapter compose.
// Uses Http::request/Http::response_status/Http::close builtins
// which get routed through the P3 client adapter.
let h = Http::request("GET", "http://127.0.0.1:18793/hello", "", "")
let status = Http::response_status(h)
Http::close(h)
status

__DATA__
{"error_contains": "Http::request"}
