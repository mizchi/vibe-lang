# Import-kind integration matrix

These fixtures record the explicit import-kind integration contract. `type`,
`struct`, `enum`, `effect`, and `trait` are exact when written. The matrix also
covers legacy bare imports, aliases, re-exports, and a qualified member imported
alongside its owning struct namespace.
