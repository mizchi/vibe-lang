let classify = (x: Int, y: Int) -> Int {
  match (x, y) {
    (a, b) if a > 0 && b > 0 => 1,
    (a, _) if a > 0 => 2,
    (_, b) if b > 0 => 3,
    _ => 0
  }
}

classify(1, 1) + classify(1, -1) + classify(-1, 1) + classify(-1, -1)

__DATA__
{"last": "6"}
