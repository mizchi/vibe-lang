test "array builder collect" {
  let b = ArrayBuilder::new()
  let _ = (ArrayBuilder::push(b, 10))
  let _ = (ArrayBuilder::push(b, 20))
  let _ = (ArrayBuilder::push(b, 30))
  let arr = ArrayBuilder::freeze(b)
  inspect(Array::get(arr, 0) + Array::get(arr, 1) + Array::get(arr, 2), "60")
}
