//# Support module for none_imported_free_var_test.vibe (#3149). A non-function
//# `let` is not generalized, so this publishes `Array[?t]` with a FREE
//# variable from this module's counter.
fn array_empty_of[T]() -> Array[T] {
  []
}

export let shared_empty = array_empty_of()
