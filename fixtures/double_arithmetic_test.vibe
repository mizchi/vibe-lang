test "double arithmetic" {
  let a = 3.14
  let b = 2.0
  let sum = a + b
  let product = a * b
  let diff = a - b
  let ratio = 10.0 / 4.0
  let int_val = Double::to_int(ratio)
  inspect(int_val, "2")
}
