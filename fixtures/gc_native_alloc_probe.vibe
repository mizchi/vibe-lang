// This program intentionally uses only a scalar result. The wasm-gc backend's
// private startup probe must still execute native struct.new and array.new
// before this user function runs, without exposing a GC reference through the
// universal i64 ABI.

export let main = () -> Int {
  42
}
