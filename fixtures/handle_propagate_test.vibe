test "handle propagate" {
  let inner = () -> Int with Exception {
    throw("propagated")
    0
  }
  let middle = () -> Int with Exception {
    inner() + 1
  }
  let result = handle {
    middle()
  } with { Exception::Throw(msg) => String::length(msg) }
  inspect(result, "10")
}
