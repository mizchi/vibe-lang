// derive(Eq) for enum

enum Color {
  Red;
  Green;
  Blue
} derive (Eq)

test "derive eq enum" {
  let a = Red
  let b = Red
  let c = Green
  let same = a == b
  let diff = a == c
  inspect(if same && not(diff) {
    1
  } else {
    0
  }, "1")
}
