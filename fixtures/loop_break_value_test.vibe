// Loop expression returning a value via break(value)

test "loop break value" {
  let sum = loop (i = 0, acc = 0) {
    if i >= 10 {
      break (acc)
    }
    continue (i + 1, acc + i)
  }
  inspect(sum, "45")
}
