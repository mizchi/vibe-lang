// Multiple struct destructuring via let
struct Vec2 { x: Int; y: Int }

let a = Vec2::{ x: 3, y: 4 }
let b = Vec2::{ x: 10, y: 20 }
let Vec2::{ x, y } = a
let sum_a = x + y
let Vec2::{ x, y } = b
let sum_b = x + y
sum_a + sum_b

__DATA__
{"last": "37"}
