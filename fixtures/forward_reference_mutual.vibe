// Forward reference: mutual recursion between two functions
let is_even = (n: Int) -> Bool {
  if n == 0 { true } else { is_odd(n - 1) }
}
let is_odd = (n: Int) -> Bool {
  if n == 0 { false } else { is_even(n - 1) }
}
is_even(10)

__DATA__
{"last": "true"}
