test "string char ops" {
  let s = "Hello"
  let len = String::length(s)
  let c0 = String::char_code_at(s, 0)
  let c4 = String::char_code_at(s, 4)
  let sub = s[1: 4]
  let result = len * 1000 + c0 * 10 + c4
  inspect(result, "5831")
}
