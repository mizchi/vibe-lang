trait Measurable {
  measure(Self) -> Int
}

impl Measurable for Int {
  measure(self) -> Int {
    self
  }
}

test "trait namespace operation dispatches" {
  assert_eq(Measurable::measure(42), 42)
}
