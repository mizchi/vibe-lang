test "string starts ends" {
  let s = "hello.vibe"
  let starts = String::starts_with(s, "hello")
  let ends = String::ends_with(s, ".vibe")
  inspect(if starts && ends {
    1
  } else {
    0
  }, "1")
}
