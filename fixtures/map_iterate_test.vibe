test "map iterate" {
  let m = MapBuilder::new()
  let _ = (MapBuilder::set(m, "a", 1))
  let _ = (MapBuilder::set(m, "b", 2))
  let _ = (MapBuilder::set(m, "c", 3))
  let frozen = MapBuilder::freeze(m)
  let keys = Map::keys(frozen)
  inspect(Array::length(keys), "3")
}
