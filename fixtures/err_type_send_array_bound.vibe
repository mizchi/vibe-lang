// ADR-0068 `Send`: Array has a mutable interior and is NOT Send in the
// initial allowlist (deep-copy contract undefined for mutable graphs).
let want_send = [T: Send](x: T) -> T { x }

fn main {
  let _ = want_send([1, 2, 3])
  ()
}

__DATA__
{"error_contains":"no impl `Send` for `Array[Int]`"}
