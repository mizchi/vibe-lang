// Scenario 5: Handle where body has no perform at all (vacuous handle)

effect Phantom {
  Boo -> Int
}

test "effect handle vacuous" {
  let result = handle {
    42
  } with { Phantom::Boo => resume(0) }
  inspect(result, "42")
}
