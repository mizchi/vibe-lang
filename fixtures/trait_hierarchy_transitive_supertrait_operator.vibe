trait LocalEq
trait LocalOrd: LocalEq
trait LocalTotalOrd: LocalOrd
impl LocalTotalOrd for Int

let accepts_eq = [T: LocalEq](value: T) -> Bool {
  true
}

let same = [T: LocalTotalOrd](a: T, b: T) -> Bool {
  accepts_eq(a) && accepts_eq(b)
}

same(1, 1)

__DATA__
{"last":"true"}
