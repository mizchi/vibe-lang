// Record access via pattern matching

test "err type record extra field" {
  let p = record {
    a: 1,
    b: 2
  }
  inspect(match p {
    record {
      a: x,
      b: y
    } => x + y
    _ => 0
  }, "3")
}
