suberror IoError {
  ReadFail
}

let read = () -> Int with IoError {
  throw(ReadFail)
  0
}

let bad = () -> Int with Exception {
  read()
}

__DATA__
{"error_contains": "effect requirements not satisfied"}
