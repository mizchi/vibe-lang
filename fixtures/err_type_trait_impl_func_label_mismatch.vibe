// #2127: `impl T for <function type>` does not parse. Function types are not
// impl targets; the labelled-argument mismatch this fixture originally named
// cannot be expressed that way. The reachable check is the parse rejection.
trait Callable
impl Callable for (x: Int, y~: String) -> Int
