// from_lines / to_lines round-trip
let text = "line1\nline2\nline3"
let lines = from_lines(text)
let count = array_length(lines)
let back = to_lines(lines)
let eq = string_equals(text, back)
eq

__DATA__
{"last": "true"}
