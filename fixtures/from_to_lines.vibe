// Lines::parse / Lines::stringify round-trip
let text = "line1\nline2\nline3"
let lines = Lines::parse(text)
let count = Array::length(lines)
let back = Lines::stringify(lines)
let eq = String::equals(text, back)
eq

__DATA__
{"last": "true"}
