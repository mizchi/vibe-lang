// ADR-0068 `Send`: a struct with a `mut` field is task-local state and
// NOT Send (structural judgment reads TDStruct's mut-field list).
struct Counter { mut n: Int }

let want_send = [T: Send](x: T) -> T { x }

fn main {
  let _ = want_send(Counter::{ n: 0 })
  ()
}
