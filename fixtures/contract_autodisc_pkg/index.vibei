// #730: a contract package with NO explicit `import ./impl.vibe {}` lines —
// the loader auto-discovers the sibling *.vibe files via Fs::readdir
// (explicit implementation imports win when present; see
// contract_sibling_impl_raws).

fn triple(x: Int) -> Int
fn shout(s: String) -> String
