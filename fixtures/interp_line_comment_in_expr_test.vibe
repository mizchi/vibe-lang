// #3022 review: a line comment inside an interpolation's expression runs to
// the newline, so a quote in it does not start a nested string.
test "a comment with a quote inside an interpolation" {
  let s = "v=\{1 // comment "
  }"
  inspect(s, "v=1")
}
