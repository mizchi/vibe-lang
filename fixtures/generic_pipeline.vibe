// Pipeline of generic functions
let identity = [T](x: T) -> T { x }
let double = (x: Int) -> Int { x * 2 }
let inc = (x: Int) -> Int { x + 1 }
identity(double(identity(inc(identity(10)))))

__DATA__
{"last": "22"}
