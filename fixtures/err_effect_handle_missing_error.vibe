effect MyEff {
  GetVal() -> Int;
}

let main = () -> Int {
  handle {
    let x = perform MyEff::GetVal()
    throw("oops")
    x
  } {
    MyEff::GetVal(k) => resume(42)
  }
}

__DATA__
{"error_contains": "effect requirements not satisfied"}
