// #1973: was fixtures/effect_higher_order_basic.vibe + __DATA__.last "18".
// Higher-order handler: function takes effectful computation as argument
effect Ask {
  Prompt(String) -> String
}

// run_with_mock: provides handler for Ask effect
// Takes a function that uses Ask, returns result with mock answers
fn run_with_mock(body: () -> String with Ask) -> String {
  handle {
    body()
  } with Ask {
    Prompt(_question) => resume("mock_answer")
  }
}

// Business logic uses Ask effect
fn greeting() -> String with Ask {
  let name = perform Ask::Prompt("What is your name?")
  String::concat("Hello, ", name)
}

fn higher_order_basic() -> Int {
  String::length(run_with_mock(greeting))
}

test "higher-order mock handler yields Hello, mock_answer" {
  inspect(higher_order_basic(), "18")
}
