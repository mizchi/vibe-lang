// `add_eq` is exported as a `let`, and imported as `trait add_eq`.
// The explicit kind should not match. Today it is accepted -- see the
// `debt-accepts` row in fixtures/typecheck/expected.tsv.
import ./modules/import_explicit_kind_lib.vibe { trait add_eq }

fn main() -> Unit {
  let _ = add_eq(1, 1)
}
