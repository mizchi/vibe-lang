// #716: dep B — exports its own `pick` (collides with dep A's) plus
// `double_pick`, whose body references the renamed sibling export: the
// defining file's internal references must follow the rename.

export let pick: () -> Int = () -> {
  2
}

export let double_pick: () -> Int = () -> {
  pick() + pick()
}
