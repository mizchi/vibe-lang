// derive(Eq) for enum
enum Color { Red; Green; Blue } derive(Eq)

let a = Red
let b = Red
let c = Green
let same = a == b
let diff = a == c

if same && not(diff) { 1 } else { 0 }

__DATA__
{"last": "1"}
