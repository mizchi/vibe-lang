// #2127 leftover: two imports binding the same local name still last-win,
// then fail with an arity mismatch of whichever builtin the alias pointed at.
import @vibe/builtin { abs }
import @vibe/builtin { clamp as abs }

abs(-1)
