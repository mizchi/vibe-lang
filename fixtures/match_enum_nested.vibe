enum Tree {
  Leaf(Int);
  Node(Tree, Tree)
}

let rec sum = (t: Tree) -> Int {
  match t {
    Leaf(n) => n,
    Node(l, r) => sum(l) + sum(r)
  }
}

sum(Node(Node(Leaf(1), Leaf(2)), Leaf(3)))

__DATA__
{"last": "6"}
