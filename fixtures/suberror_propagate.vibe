suberror AppError {
  NotFound;
  BadInput(String)
}

let inner = () -> Int with Exception {
  throw(BadInput("oops"))
  0
}

let outer = () -> Int with Exception {
  inner() + 1
}

let result = handle {
  outer()
} with Exception {
  Throw(_) => 99
}
result

__DATA__
{"last": "99"}
