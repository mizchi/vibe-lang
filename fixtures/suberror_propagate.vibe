suberror AppError {
  NotFound;
  BadInput(String)
}

let inner = () -> Int with Error {
  throw(BadInput("oops"))
  0
}

let outer = () -> Int with Error {
  inner() + 1
}

let result = handle {
  outer()
} with Error {
  Throw(_) => 99
}
result

__DATA__
{"last": "99"}
