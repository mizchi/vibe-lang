effect Console {
  Print(String) -> Unit;
  ReadLine -> String
}

test "effect perform basic" {
  let greet = () -> Unit with Console {
    let name = perform Console::ReadLine
    perform Console::Print(name)
  }
  inspect(0, "0")
}
