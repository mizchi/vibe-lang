// #2913: `String::iter_get` was admitted by the checker and emitted nowhere.
// See fixtures/err_string_iter_length_no_lowering.vibe for the shape; the hint
// here names the builtin that does read a char code.
fn main() -> Unit allows Stdout {
  println(Int::to_string(String::iter_get("abc", 0)))
}
