test "map builder" {
  let b = MapBuilder::new()
  let _ = (MapBuilder::set(b, "x", 10))
  let _ = (MapBuilder::set(b, "y", 20))
  let m = MapBuilder::freeze(b)
  let vx = Map::get(m, "x")
  let vy = Map::get(m, "y")
  inspect(vx + vy, "30")
}
