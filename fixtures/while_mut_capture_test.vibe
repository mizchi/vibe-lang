test "while mut capture" {
  let _start = () -> {
    let mut total = 0
    let mut i = 0
    while i < 5 {
      let n = i * i
      total = total + n
      i = i + 1
    }
    total
  }
  inspect(_start(), "30")
}
