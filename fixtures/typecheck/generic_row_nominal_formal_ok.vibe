// #3095 control: the caller's row names the instantiation itself.
effect State[S] {
  Get() -> S
}

fn get[S](x: S) -> Int with State[S] {
  1
}

struct Box[T] {
  v: T
}

fn caller[S](x: S) -> Int with State[Box[S]] {
  get(Box::{ v: x })
}
