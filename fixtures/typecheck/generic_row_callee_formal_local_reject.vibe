// #3053: `get("abc")` instantiates the callee's `S` at `String`, which
// `with State[Int]` does not cover.
effect State[S] {
  Get() -> S
}

fn get[S](x: S) -> Int with State[S] {
  1
}

fn caller() -> Int with State[Int] {
  get("abc")
}
