// #3042 (Codex on #3063): the dependency half of
// ordering_derive_imported_foreign_equal.vibe -- an enum with an `Equal`
// constructor but no `Less`.

export enum Match {
  Equal;
  Other
}

export fn match_of(n: Int) -> Match {
  if n == 0 {
    Equal
  } else {
    Other
  }
}
