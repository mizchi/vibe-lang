// `F` is declared as a complete type and then applied as `F[A]`. No impl can
// satisfy that -- an impl head supplying a constructor for `F` is rejected --
// so the declaration is where it is reported. Kinded `F[_]` stays legal.
trait Functor[F] {
  fmap[A, B](F[A], (A) -> B) -> F[B]
}
