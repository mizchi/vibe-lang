// Trait dispatch must preserve constructor arguments before this declaration
// can be soundly accepted. Fail closed instead of erasing F[A] to a wildcard.
trait Functor[F] {
  fmap[A, B](F[A], (A) -> B) -> F[B]
}
fn main {
  ()
}
