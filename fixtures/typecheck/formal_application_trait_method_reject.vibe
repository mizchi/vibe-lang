// Well-kinded applied formal on a trait method. The shape is declared here;
// dispatch still erases F, so a body that treats F[A] as F[B] is a later
// impl-check, not a declaration reject.
trait Functor[F] {
  fmap[A, B](F[A], (A) -> B) -> F[B]
}
fn main {
  ()
}
