// #3057: a top-level alias calls with its source's row. It was recorded with
// an empty row, so a pure caller of `h` compiled clean.
effect Tick {
  Bump() -> Int
}

fn g(x: Int) -> Int with Tick {
  x
}

let h = g

fn caller() -> Int {
  h(1)
}
