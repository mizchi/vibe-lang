// #3036 (Codex on #3076): the retained `impl [T: Builds[Err]] Use for Box[T]`
// bounds on dup_type_a_dep_ok's `Err`; `Err` here is dup_type_b_dep_ok's.
import ./dup_type_impl_mid_dep_ok.vibe { trait Use, Box }
import ./dup_type_b_dep_ok.vibe { Err }

fn read() -> Int {
  0
}
