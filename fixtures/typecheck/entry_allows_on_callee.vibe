// ADR-0088: `allows` is the row of an entry point. On a called function it
// is refused with the `with` edit (the parser names the declaration).
fn greet(name: String) -> Unit allows Console {
  println("hi \{name}")
}

fn main allows Console {
  greet("vibe")
}
