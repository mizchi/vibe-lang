// #3036 (Codex on #3076): exports an effect set whose only member is an
// operation of dup_type_effectset_qual_ea_dep_ok's `Fail`. `Fail` itself is
// not re-exported.
import ./dup_type_effectset_qual_ea_dep_ok.vibe { Fail }

export effectset Caps = { Fail::Raise }

export fn caps_arity() -> Int {
  1
}
