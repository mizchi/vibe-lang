// #3036 (Codex on #3076): an alias constructor's own binders are normalized in
// their own scope, so `Wrap { h: Holder[Inner] }` over `type Inner[X]` and
// over `type Inner[Y]` is one declaration.
import ./dup_type_alias_ctor_a_dep_ok.vibe { take_a }
import ./dup_type_alias_ctor_b_dep_ok.vibe { take_b }

fn total() -> Int {
  0
}
