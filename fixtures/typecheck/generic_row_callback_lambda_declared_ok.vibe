// #3102 review control: the lambda-declared callback's row is the lambda's
// own, so its `with State[T]` covers the call.
effect State[S] {
  Get() -> S
}

fn outer() -> Int {
  let g = [T](x: T, f: () -> Int with State[T]) -> Int with State[T] {
    f()
  }
  0
}
