// #2294 review regression: deferring validation is not enough. The stored
// alias body must also be re-resolved after the later source alias is known.
type Pair = StringMap[Int, String]
type StringMap[A, B] = (A, B)

let bad: Pair = 1

fn main {
  ()
}
