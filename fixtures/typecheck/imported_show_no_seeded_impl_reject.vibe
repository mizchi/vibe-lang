// #3022 review: an IMPORTED `trait Show` with another method is not satisfied
// at `Int` by the builtin scalar impls either.
import ./imported_show_trait_dep.vibe { Show }

fn show_it[T: Show](x: T) -> String {
  T::render(x)
}

fn main() -> Unit allows Stdout {
  println(show_it(1))
}
