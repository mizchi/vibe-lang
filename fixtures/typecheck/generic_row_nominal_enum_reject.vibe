// #3095: an enum instantiated at the caller's formal is not the formal.
effect State[S] {
  Get() -> S
}

fn get[S](x: S) -> Int with State[S] {
  1
}

enum Wrap[T] {
  W(T);
  Empty
}

fn caller[S](x: S) -> Int with State[S] {
  get(W(x))
}
