// #2967: `Async` is charged like any other label -- a row-less caller of an
// `Async` function is refused (it used to be accepted and run).
fn needs_async() -> Int with Async {
  41
}

fn pure_caller() -> Int {
  needs_async() + 1
}
