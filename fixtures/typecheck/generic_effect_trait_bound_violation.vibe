trait LocalEq
impl LocalEq for Int
let run = [T: LocalEq](x: T, f: (x: T) -> T with {Error}) -> T {
  handle { f(x) } { Error(_) => x }
}
let bad = (x: String) -> String with {Error} { x }
run("s", bad)
