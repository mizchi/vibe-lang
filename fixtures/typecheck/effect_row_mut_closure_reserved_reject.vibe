// #3045: the reservation covers a closure's own row, not only a top-level
// declaration's.
fn run() -> Int {
  let grow = (xs: Array[Int]) -> Unit with Mut {
    Array::push(xs, 9)
  }
  let ys = [1]
  grow(ys)
  Array::length(ys)
}
