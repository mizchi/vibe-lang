//# A struct field of HostStream must compile. After #2592 wired
//# named-hoststreams into CI, `desugar_derives` emitted Holder::equals which
//# called missing HostStream::equals.
struct Holder {
  s: HostStream
}

fn main() -> Int {
  0
}
