//# A struct field of HostStream must compile. After #2592 wired
//# named-hoststreams into CI, eq_for_typed called missing HostStream::equals.
struct Holder {
  s: HostStream
}

fn main() -> Int {
  0
}
