// #3018: the row of a function type written in an annotation is checked too.
fn run(f: () -> Int with Exception[Nope]) -> Int {
  handle { f() } with { Exception::Throw(_) => 0 }
}

fn main() -> Int {
  run(() -> 1)
}
