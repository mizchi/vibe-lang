// #3022 review: performing an undeclared effect does not declare it, so a
// misspelling repeated in the row and the perform is still refused.
fn log_it() -> Unit with Loger {
  perform Loger::Emit("x")
}
