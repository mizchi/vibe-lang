// #3086 (#3093 review): a `loop (acc = init)` accumulator is a value binder,
// so one spelled like the built-in `Some` is refused like a `for` variable.
fn main() -> Unit {
  let _ = loop (Some = 0) {
    if Some > 3 {
      break(Some)
    } else {
      continue(Some + 1)
    }
  }
}
