// #3036 (Codex on #3076): `Runs::run`'s own bound `Builds[Err]` names
// dup_type_a_dep_ok's `Err`; `Err` here is dup_type_b_dep_ok's.
import ./dup_type_bound_mid_dep_ok.vibe { trait Runs }
import ./dup_type_b_dep_ok.vibe { Err }

fn read() -> Int {
  0
}
