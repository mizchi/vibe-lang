// #3036 (Codex on #3076): re-exports dup_type_trait_sig_mid_dep_ok's trait
// only under an alias. The method signature names dup_type_a_dep_ok's `Err`,
// and that identity must survive the alias.
export ./dup_type_trait_sig_mid_dep_ok.vibe { trait Probe as Left }

// A function, so the fixture gate's no-entry lane has something to compile.
export fn alias_arity() -> Int {
  1
}
