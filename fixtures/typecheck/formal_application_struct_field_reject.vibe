// Well-kinded applied formal on a struct field. Constructor-only `F[Int]`
// is accepted; mixed-kind (`inner: F` together with `F[Int]`) is not.
struct Holder[F] {
  inner: F[Int]
}
fn main {
  ()
}
