//# #2820 CONTROL, the `String` twin of builtin_receiver_array_method_ok.
//# `+ 201` on a 3-byte string answers 204.
fn String::shout(s: String) -> Int {
  String::length(s) + 201
}

export fn main() -> Int {
  let s = "abc"
  s.shout()
}
