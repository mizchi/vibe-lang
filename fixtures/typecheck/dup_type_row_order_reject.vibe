// #3036 control for dup_type_row_order_ok.vibe: a row with a DIFFERENT
// member set is a different declaration, and is still refused.
import ./dup_type_row_order_a_dep_ok.vibe { Job, take_a }
import ./dup_type_row_order_c_dep_ok.vibe { Job, take_c }

fn total(j: Job) -> Int {
  take_a(j) + take_c(j)
}
