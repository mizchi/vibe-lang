// #3036 (Codex on #3076): `use_it`'s bound `Builds[Err]` names
// dup_type_a_dep_ok's `Err { code: Int }`; `Err` here is dup_type_b_dep_ok's
// `{ name: String }`, so a `Builds[Err]` witness built here would hand
// `use_it`'s compiled body the wrong layout.
import ./dup_type_bound_mid_dep_ok.vibe { use_it }
import ./dup_type_b_dep_ok.vibe { Err }

fn read() -> Int {
  0
}
