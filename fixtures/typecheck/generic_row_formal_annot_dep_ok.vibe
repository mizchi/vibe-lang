// #3051: an exported generic spelled only by its annotation -- the lambda
// repeats no `[S]` header (Codex on #3056).
export effect Tally[S] {
  Get() -> S
}

export let tally: [S](S) -> Int with Tally[S] = (x) -> { 1 }
