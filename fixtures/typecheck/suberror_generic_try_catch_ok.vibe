suberror AppError(String);

let apply_safe = [T](f: (x: T) -> T with Exception, x: T) -> T {
  handle { f(x) } with { Exception::Throw(_) => x }
}

let boom = (x: Int) -> Int with Exception {
  throw(AppError("boom"))
}

fn main {
  let _ = apply_safe(boom, 1)
}
