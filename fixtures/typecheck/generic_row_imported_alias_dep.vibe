// #3096 review: a dependency exporting an alias `N` that an importer's row
// spells, next to the generic callee from #3053.
export effect State[S] {
  Get() -> S
}

export fn get[S](x: S) -> Int with State[S] {
  1
}

export type N = String
