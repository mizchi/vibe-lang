// #3036 (Codex on #3076): the effect both halves of
// dup_type_row_formal_alpha_ok.vibe name in a field's row.
export effect Probe[T] {
  Take() -> T
}

// A function, so the fixture gate's no-entry lane has something to compile.
export fn probe_arity() -> Int {
  1
}
