// #3036 (Codex on #3076): importing a package whose sibling implementation
// shadows the contract's transparent `Err` with another module's `Err`
// (fixtures/dup_type_contract_stub_pkg/impl.vibe) is refused.
import ../dup_type_contract_stub_pkg { make }

fn read() -> Int {
  let e = make()
  e.code
}
