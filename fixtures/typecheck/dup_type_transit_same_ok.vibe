// #3036 control: `Err` reaches this module through `wrap_err`'s signature
// (declared in dup_type_a_dep_ok, imported by the middle module) and by a
// direct import of the same declaration. One type, so it stays legal.
import ./dup_type_transit_mid_dep_ok.vibe { wrap_err }
import ./dup_type_a_dep_ok.vibe { Err }

fn read() -> Int {
  let e: Err = wrap_err(3)
  e.code
}
