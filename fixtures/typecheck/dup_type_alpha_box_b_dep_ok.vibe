// #3036 (Codex on #3076): half of dup_type_alpha_equivalent_ok.vibe -- the
// same declaration as dup_type_alpha_box_a_dep_ok's `Box[T]`, its formal
// spelled `U`.
export struct Box[U] {
  value: U
}

export fn unbox(b: Box[Int]) -> Int {
  b.value
}
