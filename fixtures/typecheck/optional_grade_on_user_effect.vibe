// ADR-0088: `?` marks a host capability optional; a user effect has no host
// that could withhold it, so the grade is refused on it.
effect Ask {
  Get() -> Int
}

fn ask() -> Int with Ask::Get? {
  perform Ask::Get()
}

fn main() -> Int {
  handle { ask() } with { Ask::Get() => resume(1) }
}
