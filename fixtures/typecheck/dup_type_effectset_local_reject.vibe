// #3036 (Codex on #3076): `Fail` is over dup_type_effectset_local_dep_ok's own
// `Err { code: Int }`; `Err` here is dup_type_b_dep_ok's `{ name: String }`.
// Throwing b's value under `Fail` would hand a's handlers b's layout.
import ./dup_type_effectset_local_dep_ok.vibe { Fail }
import ./dup_type_b_dep_ok.vibe { Err }

fn boom() -> Int with Fail {
  throw(Err::{ name: "x" })
}
