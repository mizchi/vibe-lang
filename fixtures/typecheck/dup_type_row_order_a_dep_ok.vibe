// #3036 (Codex on #3076): half of dup_type_row_order_ok.vibe -- the same
// declaration with its field's effect row written one way.
export struct Job {
  run: () -> Unit with Fs + Env
}

export fn take_a(j: Job) -> Int {
  1
}
