// #3036 (Codex on #3076): a formal's spelling is not part of a declaration's
// identity inside an effect row either: `Handler[T] { run: () -> Unit with
// Probe[T] }` and the same with `U` are one declaration.
import ./dup_type_row_formal_a_dep_ok.vibe { take_a }
import ./dup_type_row_formal_b_dep_ok.vibe { take_b }

fn total() -> Int {
  0
}
