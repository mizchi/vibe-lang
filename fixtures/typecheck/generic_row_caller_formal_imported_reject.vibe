// #3053: the imported half of generic_row_caller_formal_local_reject. The
// caller's `S` does not cover the imported callee's `State[String]`.
import ./generic_row_formal_dep_ok.vibe { State, get }

fn caller[S](x: S) -> Int with State[S] {
  get("abc")
}
