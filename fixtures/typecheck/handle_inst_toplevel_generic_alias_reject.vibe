// #3027 (Codex on #3047): the same through a TOP-LEVEL alias of a generic
// declaration.
effect State[S] {
  Get() -> S
}

fn get0[S](x: S) -> Int with State[S] {
  1
}

let get = get0

fn main() -> Int {
  handle {
    get("abc")
  } with { State::Get() => resume(7) }
}
