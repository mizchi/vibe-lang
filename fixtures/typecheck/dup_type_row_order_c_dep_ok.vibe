// #3036 (Codex on #3076): half of dup_type_row_order_ok.vibe -- the same
// declaration with its field's effect row written with one member fewer.
export struct Job {
  run: () -> Unit with Fs
}

export fn take_c(j: Job) -> Int {
  1
}
