// #3053: `get([x])` instantiates the callee at the caller's `Array[S]`, which
// `with State[S]` does not cover.
effect State[S] {
  Get() -> S
}

fn get[S](x: S) -> Int with State[S] {
  1
}

fn caller[S](x: S) -> Int with State[S] {
  get([x])
}
