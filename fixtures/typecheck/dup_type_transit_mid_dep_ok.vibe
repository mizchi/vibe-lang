// #3036 control: uses dup_type_a_dep_ok's `Err` in its own signature
// without re-exporting it -- the `json_stringify.vibe` shape.
import ./dup_type_a_dep_ok.vibe { Err }

export fn wrap_err(code: Int) -> Err {
  Err::{ code: code }
}
