// #3036 (Codex on #3076): half of dup_type_row_order_ok.vibe -- the same
// declaration with its field's effect row written in another order, with a duplicate.
export struct Job {
  run: () -> Unit with Env + Fs + Env
}

export fn take_b(j: Job) -> Int {
  1
}
