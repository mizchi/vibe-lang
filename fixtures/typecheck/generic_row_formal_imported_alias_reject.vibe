// #3051 (Codex on #3056): the imported alias `get` instantiates `S` at
// `String` here, which a caller declaring `State[Int]` does not cover.
import ./generic_row_formal_alias_dep_ok.vibe { State, get }

fn caller() -> Int with State[Int] {
  get("abc")
}
