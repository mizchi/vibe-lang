// ADR-0088: Required is the join of the grade lattice -- `allows Fs` absorbs
// `Fs::read_file?`, so this `perform?` can never be `NotGranted` and is
// refused (Codex review on PR #2653).
fn main() -> Int allows Fs + Fs::read_file? {
  let _ = perform? Fs::read_file("x")
  0
}
