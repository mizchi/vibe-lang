// #2983: a kinded handler arm that does not bind its payload still names a
// type; an undeclared one was accepted when the payload was `_`.
fn k() -> Int {
  handle {
    1
  } with { Exception[Nope]::Throw(_) => 0 }
}
