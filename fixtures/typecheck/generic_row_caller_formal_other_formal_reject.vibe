// #3053: two formals of the caller are two types. `get(y)` requires
// `State[T]`, which `with State[S]` does not cover.
effect State[S] {
  Get() -> S
}

fn get[S](x: S) -> Int with State[S] {
  1
}

fn caller[S, T](x: S, y: T) -> Int with State[S] {
  get(y)
}
