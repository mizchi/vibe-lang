// #3045: a user effect may not take the reserved name `Mut`. No row spells
// the label here: a row naming `Mut` is refused by the parser before this
// declaration check runs.
effect Mut {
  Bump() -> Unit
}

fn f() -> Int {
  1
}
