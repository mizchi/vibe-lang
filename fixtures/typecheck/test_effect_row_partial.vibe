// #1508: a declared row is not a blanket exemption -- what it does not name is
// still reported. Here `Http::close` is missing from the row.
test "partial row" with Http::request {
  let r = Http::request("GET", "http://127.0.0.1:1/", "", "")
  Http::close(r)
  assert(true)
}
