// #3045: an effectset may not name the reserved `Mut` as a member either --
// that would alias the reserved label under another name (Codex on #3061).
effect Ask {
  Get() -> Int
}

effectset Writes = { Ask, Mut }
