// #3051: a generic whose row names its FIRST formal while the signature
// mentions the second one first, so an alias generalizes them in the other
// order (Codex on #3056).
export effect Slot[S] {
  Take() -> S
}

export fn f[A, B](b: B, a: A) -> Int with Slot[A] {
  1
}
