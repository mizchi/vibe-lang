// Rigid F[A] is not a concrete constructor. Ascribing it to Array[Int]
// would smash F := Array inside the polymorphic body.
fn smash[F, A](xs: F[A]) -> Int {
  let ys: Array[Int] = xs
  1
}
fn main {
  ()
}
