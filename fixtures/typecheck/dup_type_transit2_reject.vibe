// #3036: `wrap_err` reaches this module through two hops (a module that
// imported dup_type_a_dep_ok's `Err`, then a re-export), and `Err` is
// imported from dup_type_b_dep_ok. The definition must survive both hops.
import ./dup_type_transit2_mid_dep_ok.vibe { wrap_err }
import ./dup_type_b_dep_ok.vibe { Err }

fn read() -> String {
  let e: Err = wrap_err(3)
  e.name
}
