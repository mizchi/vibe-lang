// #3022 review: a type variable passed where a builtin takes an `Int` is bound
// to `Int`, so a generic wrapper is not instantiated at `String` and then read
// as an integer.
fn convert[T](x: T) -> Double {
  Int::to_double(x)
}

fn main() -> Unit allows Stdout {
  println("\{convert("abc")}")
}
