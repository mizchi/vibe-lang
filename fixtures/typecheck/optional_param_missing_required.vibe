// #1500: filling omitted optional parameters must not paper over a genuinely
// short call. Only TRAILING optionals are filled; a missing REQUIRED parameter
// still reports the real counts.
fn d(x: Int, y: Int, z?: Int) -> Int {
  x + y
}

export let bad: () -> Int = () -> {
  d(1)
}
