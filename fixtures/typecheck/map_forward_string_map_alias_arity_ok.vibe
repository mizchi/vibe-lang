// #2290: the later source alias owns the StringMap spelling, so builtin arity
// must not reject the valid two-argument application before ownership is known.
type Pair = StringMap[Int, String]
type StringMap[A, B] = (A, B)

fn main {
  ()
}
