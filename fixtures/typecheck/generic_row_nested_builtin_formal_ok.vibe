// #3095 control: the lambda passes its own formal `Int`.
effect State[S] {
  Get() -> S
}

fn get[S](x: S) -> Int with State[S] {
  1
}

fn outer() -> Int {
  let f = [Int](v: Int) -> Bool with State[Int] {
    get(v) > 0
  }
  0
}
