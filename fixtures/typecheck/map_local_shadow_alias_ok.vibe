// #2263 review regression: alias expansion preserves source ownership of Map.
struct Map[K, V] {
  key: K;
  value: V
}

type M = Map[Int, String]

fn read(m: M) -> Int {
  m.key
}

fn main {
  ()
}
