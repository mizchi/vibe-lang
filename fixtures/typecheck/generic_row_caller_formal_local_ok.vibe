// #3053 control: passing the caller's own `S` instantiates the callee at it.
effect State[S] {
  Get() -> S
}

fn get[S](x: S) -> Int with State[S] {
  1
}

fn caller[S](x: S) -> Int with State[S] {
  get(x)
}
