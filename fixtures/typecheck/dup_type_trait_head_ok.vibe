// #3036 (Codex on #3076): `run`'s bound is the trait `Builds[Err]`, so the
// dependency's private struct `Builds` takes no part, and importing another
// module's type `Builds` beside `run` is accepted.
import ./dup_type_trait_head_dep_ok.vibe { run }
import ./dup_type_trait_head_other_dep_ok.vibe { Builds }

fn total() -> Int {
  Builds::{ name: "b", size: 2 }.size
}
