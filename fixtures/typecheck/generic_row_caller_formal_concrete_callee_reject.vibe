// #3053: no generic callee is needed. A callee declaring `State[String]` is
// not covered by the caller's formal `S`.
effect State[S] {
  Get() -> S
}

fn g(x: String) -> Int with State[String] {
  1
}

fn caller[S](x: S) -> Int with State[S] {
  g("abc")
}
