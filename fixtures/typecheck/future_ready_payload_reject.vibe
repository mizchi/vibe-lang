// #3112: `Future::ready` carries its payload type, so awaiting an Int future
// into a String binding is a type error. Before, the payload was erased and
// `await` answered `?`, so this compiled.
fn read_it() -> Int with Async {
  let f = Future::ready(5)
  let s: String = await(f)
  String::length(s)
}
