// #3036: exports only a trait whose retained implementation targets
// dup_type_a_dep_ok's `Err`. `Err` itself is not re-exported.
import ./dup_type_a_dep_ok.vibe { Err }

export trait Gauge {
  gauge(Self) -> Int
}

impl Gauge for Err {
  gauge(self) -> Int {
    self.code
  }
}
