// #3002: the same for a function-typed parameter's row.
fn catch_len[T](f: () -> Int with Exception[T]) -> Int {
  handle { f() } with { Exception::Throw(_e) => 0 }
}

fn main() -> Unit allows Stdout {
  println("ok")
}
