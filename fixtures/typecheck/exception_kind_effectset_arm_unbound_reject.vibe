// #2983: nor may an unbound arm name an effectset as its kind.
suberror NotFound(String)

suberror Denied(Int)

effectset IoErrors = {
  Exception[NotFound],
  Exception[Denied]
}

fn k() -> Int {
  handle {
    1
  } with { Exception[IoErrors]::Throw(_) => 0 }
}
