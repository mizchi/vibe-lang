effect Console {
  Print(String) -> Unit
}

effect State {
  Get -> Int
}

// ERROR: function declares with Console but uses perform State::Get
let bad = () -> Int with Console {
  perform State::Get
}

fn main {
  let _ = 0
}
