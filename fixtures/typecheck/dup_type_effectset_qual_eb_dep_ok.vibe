// #3036 (Codex on #3076): a different effect `Fail`, whose operation takes
// dup_type_b_dep_ok's `Err`.
import ./dup_type_b_dep_ok.vibe { Err }

export effect Fail {
  Raise(Err) -> Unit
}

export fn eb_arity() -> Int {
  1
}
