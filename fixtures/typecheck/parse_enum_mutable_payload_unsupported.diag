fixtures/typecheck/parse_enum_mutable_payload_unsupported.vibe:2:6: parse: enum payload fields cannot be marked `mut` yet
  Ok(mut Int);
     ^~~
help: remove `mut` from enum payload type, or wrap mutability in a struct payload
note: mutable enum field syntax is tracked separately and not implemented in current grammar
