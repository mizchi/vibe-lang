//# #2475 (Codex on #2819): a suberror registers a TDEnum and binds its
//# constructors exactly as an enum does, so it recreates the same unnameable
//# nominal type. The declaration arm only ran `duplicate_ctor_errors`, so this
//# bypassed the check until the collision rule was invoked here too.
suberror Int(String)

export fn main() -> Int {
  0
}
