// #3036: re-exports dup_type_transit_mid_dep_ok's `wrap_err`, whose
// signature mentions dup_type_a_dep_ok's `Err` two hops from its declaration.
export ./dup_type_transit_mid_dep_ok.vibe { wrap_err }
