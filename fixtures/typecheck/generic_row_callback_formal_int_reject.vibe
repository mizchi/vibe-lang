// #3095: the callback's `State[S]` names `w`'s formal, not `Int`.
effect State[S] {
  Get() -> S
}

fn get[S](x: S) -> Int with State[S] {
  1
}

fn w[S](x: S, f: () -> Int with State[S]) -> Int with State[Int] {
  f()
}
