// #3036 control: the same declaration reached twice -- through `make_err`'s
// signature and through a re-export -- is one type, and stays legal.
import ./dup_type_a_dep_ok.vibe { make_err }
import ./dup_type_reexport_dep_ok.vibe { Err }

fn read() -> Int {
  let e: Err = make_err()
  e.code
}
