// #3095 control: the callback's row is the caller's own.
effect State[S] {
  Get() -> S
}

fn get[S](x: S) -> Int with State[S] {
  1
}

fn w[S](x: S, f: () -> Int with State[S]) -> Int with State[S] {
  f()
}
