// #3051: `tally("abc")` instantiates the annotation's `S` at `String`, so a
// caller declaring `Tally[Int]` does not cover it.
import ./generic_row_formal_annot_dep_ok.vibe { Tally, tally }

fn caller() -> Int with Tally[Int] {
  tally("abc")
}
