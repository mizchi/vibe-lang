// #3018: an exception kind is a type. `Exception[IoError]` with no `IoError`
// in scope used to bind the arm's payload as an opaque nominal, and
// `String::length(e)` accepted it.
export fn main() -> Int {
  handle {
    1
  } with { Exception[IoError]::Throw(e) => String::length(e) + 1000 }
}
