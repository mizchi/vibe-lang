suberror AppError(String);

let boom = (x: Int) -> Int with Error {
  if x == 0 {
    throw(AppError("boom"))
  } else {
    x
  }
}

let safe = (x: Int) -> Int {
  handle { boom(x) } with Error { Throw(_) => 0 }
}

fn main {
  let _ = safe(1)
}
