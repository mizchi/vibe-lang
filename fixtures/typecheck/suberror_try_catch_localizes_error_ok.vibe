suberror AppError(String);

let boom = (x: Int) -> Int with Exception {
  if x == 0 {
    throw(AppError("boom"))
  } else {
    x
  }
}

let safe = (x: Int) -> Int {
  handle { boom(x) } with { Exception::Throw(_) => 0 }
}

fn main {
  let _ = safe(1)
}
