// #3096 review: a callee instantiated at a tuple holding the caller's formal
// requires `State[(S, Int)]`, which the caller's `State[S]` does not cover.
effect State[S] {
  Get() -> S
}

fn get[S](x: S) -> Int with State[S] {
  1
}

fn caller[S](x: S) -> Int with State[S] {
  get((x, 1))
}
