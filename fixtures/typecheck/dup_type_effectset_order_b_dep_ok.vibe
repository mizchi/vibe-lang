// #3036 (Codex on #3076): half of dup_type_effectset_order_ok.vibe -- the same
// set as dup_type_effectset_order_a_dep_ok's `Caps`, its members in the other
// order.
export effectset Caps = { Env, Fs }

export fn home_b() -> Bool with Caps {
  Fs::exists("/")
}
