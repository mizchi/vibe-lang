// #2290: a later alias shadows the compiler-owned Future spelling. Map-key
// validation must use the complete definition table rather than declaration
// order.
type M[T] = Map[Future[T], Int]
type Future[T] = String

fn main {
  ()
}
