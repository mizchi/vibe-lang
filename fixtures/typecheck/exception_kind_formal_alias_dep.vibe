// Dependency of exception_kind_formal_alias_test.vibe: an enum the test
// imports under the alias `T`, the same spelling as its own type formal.
export enum Hue {
  Crimson;
  Teal
}

export fn hue_code(h: Hue) -> Int {
  match h {
    Crimson => 1
    Teal => 2
  }
}
