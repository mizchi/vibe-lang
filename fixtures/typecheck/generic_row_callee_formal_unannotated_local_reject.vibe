// #3053: with no row declared, the requirement names the instantiation
// (`State[String]`), not the callee's formal, which the caller cannot write.
effect State[S] {
  Get() -> S
}

fn get[S](x: S) -> Int with State[S] {
  1
}

fn caller() -> Int {
  get("abc")
}
