// #3036: as dup_type_nested_reject, with a's `Outer` one hop away, so the
// nested `Inner` must travel with `Outer`'s reference definition.
import ./dup_type_nested_mid_dep_ok.vibe { wrap_outer }
import ./dup_type_nested_b_dep_ok.vibe { consume_outer }

fn read() -> String {
  consume_outer(wrap_outer())
}
