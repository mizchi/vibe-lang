// #3036 (Codex on #3076): half of dup_type_row_formal_alpha_ok.vibe -- its
// formal `T` appears inside a function field's effect row.
import ./dup_type_row_formal_eff_dep_ok.vibe { Probe }

export struct Handler[T] {
  run: () -> Unit with Probe[T]
}

export fn take_a(h: Handler[Int]) -> Int {
  1
}
