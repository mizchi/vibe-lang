// #3095 control: an imported struct in the row names the instantiation.
import ./generic_row_nominal_dep.vibe { State, get, Box }

fn caller[S](x: S) -> Int with State[Box[S]] {
  get(Box::{ v: x })
}
