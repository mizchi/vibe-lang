// #3044 / ADR-0117: a bare `_` argument outside the call directly after
// `|>` is neither a pipe slot nor a section, and is refused.
fn inc(x: Int) -> Int {
  x + 1
}

fn apply5(f: (Int) -> Int) -> Int {
  f(5)
}

fn run() -> Int {
  apply5(inc(_))
}
