// Codex on #3021: the row of a function type in a STRUCT FIELD names an
// exception kind too, and an undeclared one is refused there.
struct H {
  run: () -> Int with Exception[Nope]
}

fn main() -> Int {
  0
}
