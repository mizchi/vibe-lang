// #3036 (Codex on #3076): the retained `impl Decoder[Err] for Token` means
// dup_type_a_dep_ok's `Err { code: Int }`; `Err` here is dup_type_b_dep_ok's
// `{ name: String }`, so `Token::decode` would hand back a's layout as b's.
import ./dup_type_impl_mid_dep_ok.vibe { trait Decoder, Token }
import ./dup_type_b_dep_ok.vibe { Err }

fn read() -> Int {
  0
}
