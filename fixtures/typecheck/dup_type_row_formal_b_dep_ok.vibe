// #3036 (Codex on #3076): half of dup_type_row_formal_alpha_ok.vibe -- the
// same declaration, its formal spelled `U`, in the row too.
import ./dup_type_row_formal_eff_dep_ok.vibe { Probe }

export struct Handler[U] {
  run: () -> Unit with Probe[U]
}

export fn take_b(h: Handler[Int]) -> Int {
  2
}
