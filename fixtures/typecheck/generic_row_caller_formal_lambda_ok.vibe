// #3053 control: the nested lambda passes its own `T` to the callee.
effect State[S] {
  Get() -> S
}

fn get[S](x: S) -> Int with State[S] {
  1
}

fn caller() -> Int with State[String] {
  let inner = [T](y: T) -> Int with State[T] {
    get(y)
  }
  inner("abc")
}
