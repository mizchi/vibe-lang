// #3101 control: the inherited `with State[S]` names the outer `S`, and
// `get(x)` passes the outer `S`.
effect State[S] {
  Get() -> S
}

fn get[S](x: S) -> Int with State[S] {
  1
}

fn caller[S](x: S) -> Int with State[S] {
  let g = [S](y: S) -> Int {
    get(x)
  }
  0
}
