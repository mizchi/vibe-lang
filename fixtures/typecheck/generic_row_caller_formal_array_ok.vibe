// #3053 control: the row names the instantiation the call makes.
effect State[S] {
  Get() -> S
}

fn get[S](x: S) -> Int with State[S] {
  1
}

fn caller[S](x: S) -> Int with State[Array[S]] {
  get([x])
}
