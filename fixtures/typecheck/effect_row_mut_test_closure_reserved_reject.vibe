// #3045: a closure inside a `test` block may not name the reserved `Mut`
// either, even one that is never called (Codex on #3061).
test "grows" {
  let grow = (xs: Array[Int]) -> Unit with Mut {
    Array::push(xs, 9)
  }
  let _ = grow
  assert(true)
}
