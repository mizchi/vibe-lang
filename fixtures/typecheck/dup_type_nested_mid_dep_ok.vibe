// #3036: imports `Outer` without re-exporting it and exports only a
// function whose signature mentions it, so `Outer` and the `Inner` it holds
// travel only as reference definitions.
import ./dup_type_nested_a_dep_ok.vibe { Outer, make_outer }

export fn wrap_outer() -> Outer {
  make_outer()
}
