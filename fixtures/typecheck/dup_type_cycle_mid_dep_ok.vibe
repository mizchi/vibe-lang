// #3036: mentions dup_type_cycle_dep_ok's `Node` in a signature without
// re-exporting it, so the cyclic closure travels as reference definitions.
import ./dup_type_cycle_dep_ok.vibe { Node, make_node }

export fn wrap_node() -> Node {
  make_node()
}
