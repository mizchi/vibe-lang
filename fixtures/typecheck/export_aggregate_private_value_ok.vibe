//# #2762 green side: the aggregate export's own reason for existing -- a
//# private `let` / `fn` published without `export` on the declaration.

export {
  secret,
  helper
}

let secret = 7

fn helper() -> Int {
  3
}

export fn main() -> Int {
  secret + helper()
}
