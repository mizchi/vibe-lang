// #3095: a non-generic struct is not the caller's formal either.
effect State[S] {
  Get() -> S
}

fn get[S](x: S) -> Int with State[S] {
  1
}

struct Pt {
  x: Int
}

fn caller[S](x: S) -> Int with State[S] {
  get(Pt::{ x: 1 })
}
