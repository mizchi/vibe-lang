// #3036: exports only a trait whose method signature mentions
// dup_type_a_dep_ok's `Err`. `Err` itself is not re-exported.
import ./dup_type_a_dep_ok.vibe { Err }

export trait Probe {
  probe(Self, Err) -> Int
}

impl Probe for Int {
  probe(self, e: Err) -> Int {
    self + e.code
  }
}
