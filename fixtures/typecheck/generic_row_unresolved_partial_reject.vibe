// #3095: `get([])` leaves the element open, but an array is never the caller's
// formal `S`. The requirement used to keep the callee's spelling `State[S]`.
effect State[S] {
  Get() -> S
}

fn get[S](x: S) -> Int with State[S] {
  1
}

fn caller[S](x: S) -> Int with State[S] {
  get([])
}
