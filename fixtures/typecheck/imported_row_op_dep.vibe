// Sibling module for imported_row_op_typo_reject (#3022 review): exports a
// function whose row names one operation of `Ask`, and not the declaration.
effect Ask {
  Get() -> Int
  Put(Int) -> Unit
}

export fn ask() -> Int with Ask::Get {
  perform Ask::Get()
}
