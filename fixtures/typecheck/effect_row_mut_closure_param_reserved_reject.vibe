// #3045: a nested closure's parameter annotated with a function type may not
// name the reserved `Mut` in that type's row (Codex on #3061).
fn run() -> Int {
  let apply = (cb: () -> Unit with Mut) -> Unit {
    ()
  }
  let _ = apply
  1
}
