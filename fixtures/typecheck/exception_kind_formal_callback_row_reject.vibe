// #3002: the same for a function type's row in a parameter annotation.
fn catch_len[T](f: () -> Int with Exception[T]) -> Int {
  handle { f() } with { Exception::Throw(_) => 0 }
}

fn fail_str() -> Int with Exception[String] {
  throw("abc")
}

fn main() -> Int {
  catch_len(fail_str)
}
