// #3036 (Codex on #3076): `Caps` is `{ Fail::Raise }` over
// dup_type_effectset_qual_ea_dep_ok's `Fail`, reached only through the
// qualified member's head. The `Fail` imported here is a different
// declaration, so a `with Caps` row would name one effect and a handler the
// other.
import ./dup_type_effectset_qual_mid_dep_ok.vibe { Caps }
import ./dup_type_effectset_qual_eb_dep_ok.vibe { Fail }

fn call() -> Int with Caps {
  1
}
