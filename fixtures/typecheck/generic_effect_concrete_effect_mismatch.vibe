let chain = [T](
  f: (x: T) -> T with Console,
  g: (x: T) -> T with Exception,
  x: T,
) -> T with Console {
  g(f(x))
}
let f = (x: Int) -> Int with Console { x }
let g = (x: Int) -> Int with Exception { x }

fn main {
  let _ = chain(f, g, 1)
}
