// #3027 (Codex on #3047): the same, with the generic lambda declared INSIDE
// the handled body.
effect State[S] {
  Get() -> S
}

fn main() -> Int {
  handle {
    let get = [S](x: S) -> Int with State[S] {
      1
    }
    get("abc")
  } with { State::Get() => resume(7) }
}
