// #3036 (Codex on #3076): an effect `Fail` whose operation takes
// dup_type_a_dep_ok's `Err`.
import ./dup_type_a_dep_ok.vibe { Err }

export effect Fail {
  Raise(Err) -> Unit
}

export fn ea_arity() -> Int {
  1
}
