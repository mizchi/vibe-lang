// #3022 review: only the parser's own ascription wrapper names a throw
// payload's kind. Another immediately-applied lambda annotates its ARGUMENT,
// so this Int payload is not read as a String.
fn boom() -> Int with Exception[String] {
  throw(((x: String) -> Int { 1 })("s"))
}
