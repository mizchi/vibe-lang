// #1515 guard: the alias resolution must not over-reject. A local alias of a
// PURE function carries no row, so the caller needs none.
fn pure_fn(s: String) -> String {
  s
}

fn caller() -> String {
  let f = pure_fn
  f("ok")
}
