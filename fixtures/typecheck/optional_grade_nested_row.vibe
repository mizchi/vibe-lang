// ADR-0088: the grade check reaches every function-type row, not only a
// binding's own -- a `?` on a user effect in a type alias is the same claim
// about nothing (Codex review on PR #2653).
effect Ask {
  Get() -> Int
}

type Callback = () -> Int with Ask::Get?

fn main() -> Int {
  0
}
