// #3095: the same nominal head at different arguments names different types.
effect State[S] {
  Get() -> S
}

fn get[S](x: S) -> Int with State[S] {
  1
}

struct Box[T] {
  v: T
}

fn caller() -> Int with State[Box[Int]] {
  get(Box::{ v: "s" })
}
