// #3045: `Mut` is reserved for a future argument-mutation marker, so a row
// naming it is refused today rather than read as a user effect.
fn grow(xs: Array[Int]) -> Unit with Mut {
  Array::push(xs, 9)
}
