// #3036: a trait implemented for this module's own `Err`, which is a
// different declaration from dup_type_a_dep_ok's `Err`.
export struct Err {
  name: String
}

export trait Weigh {
  weigh(Self) -> Int
}

impl Weigh for Err {
  weigh(self) -> Int {
    String::length(self.name)
  }
}
