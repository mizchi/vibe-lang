// #3112: `Future::pending()`'s payload is settled by what resolves it, and
// `await` answers that type.
fn read_it() -> Int with Async {
  let f = Future::pending()
  Future::resolve(f, 40)
  let n: Int = await(f)
  n + 2
}
