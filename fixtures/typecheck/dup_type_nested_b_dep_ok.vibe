// #3036: an `Outer` spelled exactly like dup_type_nested_a_dep_ok's, holding
// a DIFFERENT `Inner`.
export struct Inner {
  name: String
}

export struct Outer {
  inner: Inner
}

export fn consume_outer(o: Outer) -> String {
  o.inner.name
}
