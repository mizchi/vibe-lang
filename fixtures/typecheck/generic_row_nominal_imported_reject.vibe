// #3095: the same shape as generic_row_nominal_formal_reject, with the struct
// and the callee imported.
import ./generic_row_nominal_dep.vibe { State, get, Box }

fn caller[S](x: S) -> Int with State[S] {
  get(Box::{ v: x })
}
