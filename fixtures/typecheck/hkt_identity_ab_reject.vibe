// #2268: returning F[A] as F[B] is not map. Rigid formals keep A and B distinct.
fn fake_map[F, A, B](x: F[A], f: (A) -> B) -> F[B] {
  x
}
fn main {
  let xs = [1, 2, 3]
  let _ys: Array[String] = fake_map(xs, (n: Int) -> String {
    "x"
  })
  ()
}
