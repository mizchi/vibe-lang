// #3101 control: the lambda passes its OWN `S`, which its `with State[S]`
// names.
effect State[S] {
  Get() -> S
}

fn get[S](x: S) -> Int with State[S] {
  1
}

fn caller[S](x: S) -> Int {
  let g = [S](y: S) -> Int with State[S] {
    get(y)
  }
  0
}
